# Active Context

- **2026-09-04 (backend+agent runtime / hidden Codex fantasy player MVP, deployed but not activated):** Commits `c4d2533` and `4910104` publish the internally marked AI-managed user, public MCP documentation, backend Flyway V83, Python 3.10 `agent-runtime`, and an activation-ready disabled system installer. Production VPS is on `4910104`, backend is healthy, Flyway V83 succeeded, all three new tables exist, and `api_credential` has zero rows. Runtime provides closed Fantasy/Research/Memory MCP registries, immutable Polemica evidence with durable COLLECT→SEAL snapshots, decision-bound single-attempt intents, long-term SQLite/blob memory, hourly Codex runner and hardened systemd artifacts. Runtime tests pass 106/106; backend focused compile/unit and remote Docker/PostgreSQL Testcontainers auth/pack concurrency tests pass. A no-secret disabled copy is staged on `codex@51.250.97.185`; root SSH is rejected and `codex` sudo requires a password, so the dedicated broker user/system units cannot be installed non-interactively. No production account, Bearer credential, secrets, active MCP services, timer, or agent write/run exists.
- **2026-09-04 (backend+TMA / Polemica Grand Slam card skin):** Added the pack-assignable `polemica_grand_slam_2026` card skin through Flyway V82 and implemented the selected electric-blue/cyan treatment across collection, team, modal, marketplace/transaction, pack reveal, and pack-summary surfaces. The existing card geometry remains unchanged; the skin adds a `GRAND SLAM` badge, animated blue frame/glow/sweep, blue name treatment, and matching perk chips with reduced-motion fallbacks. Backend/test compilation and the TMA production build passed; a browser preview confirmed the intended skin class, badge, glow, and sweep. Not deployed yet.
- **2026-08-28 (DX / API-only series announcement skill):** Добавлен отдельный переносимый skill `.codex/skills/polemica-api-create-series-from-announcement` для администраторов без доступа к репозиторию, VPS и production DB. Workflow использует только HTTPS Basic Auth admin API: fresh GET preflight турниров/ростера/серий, точный `find-player` по primary/alias Polemica ID, добавление отсутствующего подтверждённого игрока по существующему `fantasyPlayerId` или точному `polemicaUserId`, dry-run create/assignment, отдельные POST и независимую GET-проверку. Stdlib helper не принимает credentials в CLI, разрешает writes только через глобальный `--execute`, создаёт только `UPCOMING`, требует ожидаемый backend-derived `publicNumber` и явный `gamePhase` для competition (`2` обычно для финала), валидирует kind-specific payload/полный roster replacement. Resume существующей серии разрешён только при полном совпадении полей и полностью пустом API-состоянии; отдельный operation ledger фиксирует частично committed add/create/assign и запрещает автоматический rollback/retry. Исходный внутренний DB/SSH skill не изменён.
- **2026-08-28 (backend+production / fantasy player import opt-out):** Polemica player `41582` was removed from production tournament `39`; no series/cards or other FK-backed history referenced the player, and the tournament plus its all-player pack now resolve to 19 eligible players. Backend Flyway V81 adds a persistent Polemica-ID import blocklist seeded with `41582`, conditionally deletes the now-orphan global profile, and rejects both a blocked direct ID and an existing Fantasy player carrying a blocked alias on future tournament roster additions. Policy unit tests and backend/test compilation passed; the Testcontainers API scenario compiled but could not run without a Docker daemon. The code and migration are not deployed yet.
- **2026-08-28 (backend+production / RESULT identity aliases):** Telegram RESULT identity matcher now canonicalizes the observed cross-script aliases `фокс -> fox` and mixed-script `ОloF -> olof` for both sheriff and mafia-line validation. Focused `SeriesCompletionServiceTest` passed. A clean `c76757b` snapshot plus only this source change was deployed as lightweight backend image `2b92632099c8...`; rollback image `rollback-pre-identity-aliases-20260828` retains the previous `f0049a94fd03...` backend. Health and public TMA/Admin HTTP are good. Production readiness became READY for ЗЛ №28 (`series_id=254`); ЛП №41 (`series_id=256`) was then explicitly confirmed by Telegram actor `831232089` and finalized through the queued standard job, not by the deploy operation itself.
- **2026-08-26 (backend+production / ЗЛ OCR alias `Doc.`):** ZL roster policy now binds the punctuation-preserving map key `[Doc.] -> Doc`; a binding regression test protects Spring Boot's bracket syntax, and the resolver exact-match test protects the resulting `Doc.` OCR mapping. Resolver version v4 lets same immutable evidence refresh only unapplied ANNOUNCEMENT items when their stored resolver version is stale; applied announcements and ordinary same-evidence deliveries remain idempotent. Focused unit + Testcontainers regressions passed. Backend image `f0049a94fd03...` is healthy with rollback `rollback-pre-doc-alias-resolver-v4-20260826`. Targeted resend of stored post `2292` evidence resolved all 10 players including `Doc.` -> `Doc`, preserved the caption substitution `Монарх` -> `Воробей`, moved item 25 to `READY_TO_PREVIEW`, and edited operator message `218624` to delivered `CREATE_PREVIEW`; global policy generation and unrelated actions/results were untouched.
- **2026-08-26 (Telegram import / OCR supersede recovery):** Production announcement `2292` (ЗЛ №28) was classified correctly but its first OCR task became terminal `SUPERSEDED` after Telegram reported changed source media/version; the unchanged semantic fingerprint then advanced only the message source version and never requeued OCR, so no backend/operator outbox existed. A targeted `reclassify-message --message-id 2292` replay delivered one backend `ROSTER_REVIEW` notification (`telegram_message_id=218605`) and left the item safely `NEEDS_REVIEW` because OCR resolved 9/10 players. Worker storage now requeues only `SUPERSEDED` source-media OCR when the same semantic revision is observed with a strictly newer source version; same-version duplicates and terminal `FAILED`/`UNSUPPORTED` remain closed. All 63 worker tests passed. Production worker image `e2656cf7df61...` is healthy with rollback tag `rollback-pre-ocr-requeue-20260826`; backend is `UP`, watermark is `2292`, and worker/backend/operator queues have zero pending/failed rows.
- **2026-08-24 (backend+admin / tournament default expected games):** Турнир получил nullable `defaultExpectedGameCount` (`1..1000`) в Flyway V80, admin API и UI. При создании серии отсутствующий или explicit `null` `expectedGameCount` копирует текущий default турнира, явное число остаётся override; дальнейшее изменение/очистка настройки турнира не переписывает существующие серии. Admin-форма серии показывает и подставляет default, а update турнира различает omitted / value / explicit null. Backend compile, DTO unit test и admin production build прошли; `AdminApiIntegrationTest` с полным inheritance/update сценарием скомпилирован, но локально не запустился без Docker daemon. Production DB verification on 2026-08-26 confirmed Flyway V80 was applied at `2026-08-24 11:30:08Z`; current admin static rollout was not re-verified.
- **2026-08-24 (backend+admin+production / audited Telegram RESULT correction):** Добавлен immutable per-game override ошибочной строки `Мафия` поверх неизменяемого Telegram RESULT evidence: admin API/UI принимает коррекцию только если она точно совпадает с текущими ролями DON/MAFIA из Polemica cache, сохраняет исходное/новое значение, причину и Basic Auth actor, пишет `RESULT_MAFIA_OVERRIDDEN` audit и включает override в readiness checksum. Flyway V79 применён production rollout через lightweight JAR overlay image `9458706b4530...`, rollback image `rollback-pre-result-mafia-override-20260824`; admin static backup сохранён в `/tmp/polemica-result-mafia-override-20260824/admin-before.tar.gz`. Для ЛП №39 (`series_id=246`) исправлен дубликат `Beach House` на `Якорь` в игре 2, readiness стал READY, серия штатно финализирована: 193 reward transactions на 9 588₣ и 568 списаний uses; backend/DB/worker healthy.
- **2026-08-17 (backend + production / Telegram LP-ZL operator flow):** Ручное создание серии теперь использует одно редактируемое сообщение оператора для `CREATE_PREVIEW -> CREATE_CONFIRM -> CREATED -> ACTIVE`; Bot API `editMessageText` повторяется через durable outbox, `message is not modified` считается идемпотентным успехом, а удалённое/нередактируемое сообщение заменяется один раз. После создания появляется отдельное signed-действие `Перевести в ACTIVE`, привязанное к тому же оператору и message id; backend разрешает только locked `UPCOMING -> ACTIVE` и сохраняет стандартный `SeriesBatchStartedEvent`. `RESULT`/финализация остаётся отдельным Telegram-сообщением, которое также редактируется внутри своего preview/confirm/final outcome flow. Commit `e2f796b` развёрнут lightweight JAR overlay image `432974d7aaf6...`, Flyway V77 applied, backend/worker/DB healthy; LP/ZL create+finalize остаются `MANUAL`, все gates/notifications включены. Тяжёлая VPS-сборка снова перегрузила VM; после control-plane reboot устранён boot deadlock guard-before-Docker (`docker network inspect` заменён static CIDR fallback до старта daemon), backup: `/usr/local/sbin/polemica-telegram-egress-guard.rollback-20260817`, image rollback: `polemica-fantasy-backend:rollback-pre-telegram-flow-20260817`.
- **2026-08-16 (backend / additive STANDALONE game membership):** `DefaultGameSyncService` снова сохраняет уже привязанные к `STANDALONE`-серии игры при последующих изменениях `namePrefix`, `gameStartedOn` или ростера. Новый selector используется только для добавления/обновления совпавших игр; ошибочную старую привязку можно удалить вручную через admin API. Для `POLEMICA_COMPETITION` диапазон по-прежнему является authoritative snapshot и удаляет игры вне актуальной выборки.

## Changelog
- **2026-08-16 (observability config hardening, not yet uploaded to MSP):** tracked `FantasyActiveSeriesSchedulerStale` now also recognizes `completion > success`, so a cold-start repository failure cannot be hidden by the initial zero active-series gauges. The rule retains the 20-minute grace period through `process_uptime_seconds`, and the scheduler test covers the fatal repository path. Monium logging docs were aligned with Fluent Bit `5.1.0` and the notification runbook now states that missing recipients count as delivery errors.
- **2026-08-16 (backend / fantasy player merge):** `REPLACEMENT_ALIAS_CONFLICTS` теперь учитывает только нефинализированные серии: историческая замена на alias объединяемого профиля в finalized series сохраняется для аудита и больше не блокирует merge. Активные/незавершённые серии по-прежнему блокируются; добавлены integration-сценарии для обеих веток.
- **2026-08-16 (backend+admin / manual series finalization):** Admin readiness/finalization now allows fully ready `UPCOMING`, `ACTIVE`, or `SCORING` series while Telegram result automation retains its strict `SCORING` + checksum fence. The admin confirmation no longer exposes or submits a snapshot hash; the backend recomputes readiness under the series lock. `SERIES_FINALIZED` achievement recomputation moved to an isolated best-effort async listener so committed finalization no longer waits on every participant, admin reconciles ambiguous HTTP errors by rereading the series, and the tracked admin Nginx config uses 300-second proxy read/send timeouts as a fallback.
- **2026-08-14 (production infra / inbound Telegram webhook relay):** Production Bot API webhook switched from the Yandex-hosted public endpoint to `https://tg-hook.maftourbot.ru/api/v1/telegram/webhook` on VPS2 `91.108.240.119`. VPS2 Nginx terminates Let's Encrypt TLS, accepts only the exact POST path from current official Telegram webhook CIDRs, preserves the secret header, limits body to 1 MB, uses 2s/10s/35s proxy timeouts, and forwards over `wg2 -> VPS1 wg0` to a WireGuard-only Fantasy listener at `10.10.0.4:18082`. VPS1 forwarding and Fantasy input are limited to the exact source/destination/port and persisted in WireGuard routing hooks. Cutover preserved pending updates: one real Telegram update was delivered with `200`, queue drained to zero, `last_error` cleared, and a synthetic `lic:` callback was durably processed once in `league_import_callback_inbox`. Certbot renewal dry-run passed; one-command rollback is `/usr/local/sbin/polemica-webhook-rollback` on the Fantasy VPS.
- **2026-08-11 (Telegram import / delivered-notification barrier + terminal safety):** Automatic CREATE/FINALIZE jobs are now fenced by the durable pending operator outbox: a production job receives `availableAt = deliveredAt + 120s` only after Telegram successfully accepts `AUTO_*_PENDING`; Bot API outage leaves it unleaseable. Disabled/global-off/mode/generation mismatches sweep jobs/actions to terminal `CANCELLED`, clear leases and supersede pending notices, so same-generation re-enable cannot revive them; read-only manual preview is preserved when only the write gate is off. Source edits cancel stale job leases after the evidence transaction commits, avoiding evidence→job lock inversion. Sync and scoring persist separate game-selector fingerprints and completion rejects changed selectors/aliases. Cancellation messages include league, series, source, mode and generation.
- **2026-08-11 (Telegram import / admin readiness + automation pending UX):** Admin series create/edit now carries nullable `expectedGameCount` (required before `SCORING`) and no longer exposes `FINISHED` as an editable status. Finalization uses a readiness preview followed by an exact checksum-bound confirmation, so any evidence/scoring/economy change rejects the write. Automatic create/finalize emit pending notifications with ETA/mode/policy generation; auto-finalize has an additional 120-second cancellation hold. Startup validation requires ingest, production writes and operator notifications for automatic operations, plus result processing for automatic finalize. Manual Telegram actions are bound to policy generation and are durably cancelled when mode/generation changes.
- **2026-08-11 (Telegram import / Russian textual dates + targeted replay):** Announcement classifier rule v2 теперь принимает как numeric dates (`11.08`), так и русские genitive month names (`11 августа`). Добавлена идемпотентная operator-команда `reclassify-message --message-id`, которая под session lock повторно читает ровно один pinned-channel post, создаёт audit revision с classifier rule version и не переотправляет исторический inbox массово. Production message `2234` (`ЛП`, серия 34) пере-классифицирован `IGNORE -> ANNOUNCEMENT`; создан ровно один `CANDIDATE`, notification `DELIVERED`. После daemon restart: healthy, restart count 0, message имеет 2 revisions и 1 delivered outbox event, повторный scan дубль не создал; pending/failed outbox = 0, backend `UP`. Перед replay сохранена согласованная `0600` backup session/inbox.
- **2026-08-11 (Telegram import / operator notifications enabled):** Production shadow-worker настроен на существующий Fantasy bot и приватный операторский чат без forum topic. Bot token скопирован только между restricted VPS env files и не выводился/не попадал в repo; import env остаётся `0600`, перед изменением создана `0600` backup. `notify-test` успешно доставлен через VPN-only network, daemon пересоздан и healthy (`notificationsEnabled=true`, restart count 0). После включения worker доставил 2 накопившихся shadow-candidate события; pending/failed outbox = 0, backend `UP`, worker logs не содержат token patterns. Это по-прежнему только `[SHADOW][NO PRODUCTION WRITE]`: actual create/finalize notifications ещё не реализованы и должны исходить из backend `AFTER_COMMIT`.
- **2026-08-07 (Telegram import / production shadow worker):** На production VPS запущен постоянный structurally `SHADOW`-only Telethon worker для `@polemica_closed_league`: отдельный daemon Compose service без backend/admin/DB credentials, только VPN external network, pinned account/channel identity и session lifetime lock. Первый bounded backfill сохранил 100 сообщений/ревизий в crash-safe SQLite (`WAL`, `synchronous=FULL`, schema v1, `0600`): 22 `RESULT`, 78 `IGNORE`; повторный canary не создал дублей, outbox пуст. Worker healthy, polling heartbeat обновляется, restart count 0, backend `UP`; read-only DB pre/post подтверждает неизменные 217 series / 137 finalized. Operator Bot API notifications оставлены выключенными до выбора отдельного приватного chat/topic и `notify-test`; worker не создаёт и не финализирует серии, а реальные lifecycle notifications позже должны исходить из backend `AFTER_COMMIT`.
- **2026-08-07 (Telegram import / production Telethon authorization):** Dedicated user session успешно авторизована на production VPS и проверена чтением allowlisted `@polemica_closed_league`; numeric account/channel identity и последний доступный message ID сохранены без вывода содержимого. Session directory имеет `0700`, session/lock/metadata — `0600`. Worker использует только external network `172.24.0.0/28`; source route ведёт весь её egress через table 201 / `wg-tg`, nftables guard и repair timer active. One-shot auth container штатно завершён, Fantasy backend остался `UP`; постоянный polling/shadow-ingest был следующим шагом и отражён в записи выше.
- **2026-08-06 (Telegram import / Telethon auth bootstrap):** Подготовлен, но ещё не установлен на production, opt-in Compose profile для интерактивной авторизации dedicated Telegram user account и проверки чтения `@polemica_closed_league`. API credentials вынесены из backend `.env`, session хранится в отдельном bind mount с `0700/0600`, account/channel identity фиксируется и повторно проверяется. Для строгого VPS VPN egress worker изолирован в external Docker network `172.24.0.0/28`: всё её IPv4-направление уходит в table 201, а независимый nftables base chain fail-closed отклоняет любой выход не через `wg-tg`; systemd устанавливает guard до Docker, timer восстанавливает его, wrapper повторно применяет guard непосредственно перед login. Реальные credentials, session и host firewall/network changes ещё не создавались.
- **2026-08-06 (discovery refinement / Telegram series autopilot):** Импорт анонсов переработан под конкретный allowlisted канал `@polemica_closed_league` и два связанных event flow: `ANNOUNCEMENT -> reviewed UPCOMING series` и `RESULT -> sync -> reconciliation -> scoring -> READY_TO_FINALIZE`. Из-за отсутствия channel-admin доступа выбран read-only Telethon worker под отдельной user session с polling/backfill; Telegram остаётся completion evidence, а Polemica — source of truth для игр и очков. Для OCR выбран проверяемый на golden corpus Yandex Vision (`page`, `ru/en`, отдельный `ai.vision.user` service account). Concurrent finalize теперь сериализуется pessimistic row lock; обычные sync/rescore отклоняются после finalize и повторно проверяют состояние под lock после HTTP. Экономическая финализация в MVP остаётся ручной; unattended finalize отложен до readiness/idempotency/correction guards и shadow evidence.
- **2026-08-06 (discovery / scouting lab + announcement import):** Зафиксирован единый product/technical brief `docs/features/IDEAS-SCOUTING-LAB-AND-ANNOUNCEMENT-IMPORT.md`. «Скаутская лаборатория» отделена от scoring replay: это mobile-first предматчевый анализ и подбор допустимого состава по детерминированной истории, с data-quality audit, uncertainty и без online Polemica calls. Первоначальный paste/image-first импорт зафиксировал базовые safety-инварианты: structured extraction без production tools, deterministic enrichment, human review, duplicate/roster impact dry-run, idempotent explicit apply и read-only verification; channel-driven scope уточнён в следующей записи выше.
- **2026-08-06 (production observability / direct Monium Logs):** Backend production logs now go directly to Monium as structured Logstash JSON through dedicated systemd `polemica-monium-logs.service` on Fluent Bit `5.1.0`; Cloud Logging and Grafana are not in this path. `logger-robot` received only `monium.logs.writer`, and API key `ajeo1kns6trm1ga75luu` has only `yc.monium.logs.write` scope and is stored root-only on the VM. Spring file rotation is 10 MB/file, 100 MB total, 7 days; Fluent Bit filesystem queue is capped at 100 MB. Explicit Telegram/internal user identifiers were removed from application log messages, Telegram transport exceptions drop token-bearing URI causes, and the Polemica library is held at WARN because its INFO auth-refresh message contained the configured username. The first acceptance query exposed one such pre-mitigation email record; after the immediate config fix and backend restart, Monium showed 49 fresh records including startup with zero email, explicit user/chat ID or bot-token patterns. Backend health, Unified Agent and the dedicated forwarder are healthy; generic legacy `fluent-bit.service` stays disabled. Runtime image is `f4e2f23c8412...`, rollback is `rollback-pre-monium-20260806`; server git checkout was not modified.
- **2026-08-06 (backend / onboarding notification delivery):** Production `onboarding_tips; error` traced to 55 unreachable Telegram chats returning `400 Bad Request: chat not found`: 476 failures over 7 hourly scheduler runs, including 13 recipients selected by two nudge segments per run. Exact `chat not found` responses are now treated as permanently unreachable, persisted through the existing `bot_blocked` suppression path, and re-enabled by the existing TMA login reset; unrelated Telegram `400` responses remain errors. Notification logs now include category. Targeted Telegram client and delivery tests pass; fix was deployed with the direct Monium Logs rollout.
- **2026-08-05 (backend + production observability / domain metrics):** Добавлен central `FantasyMetrics` facade с low-cardinality метриками sync/scoring/finalization/notifications/schedulers и AFTER_COMMIT учётом финализации; notification delivery считает конечный outcome и продолжает batch после preparation error. Production backend развёрнут lightweight JAR overlay image `46f0ab72f566...`, rollback image сохранён, server git checkout не менялся. Unified Agent переведён на exact `fantasy_*` allowlist: live 36 domain / 127 app / 33 host series, calculated domain worst case 135, total gate 450. В MSP загружены три domain rules (все `OK`) и создан dashboard `polemica-fantasy-production` (`fbec6eh0cj6qn8u95p6r`) с 8 Managed Prometheus widgets; после удаления ошибочно сериализованного target-level `step: "auto"` все 8 виджетов проверены в Monium с реальными графиками и без Prometheus Remote API errors. Тяжёлая первая Docker/Gradle-сборка перегрузила VM и потребовала control-plane reboot; пять работавших до reboot legacy-контейнеров восстановлены, дальнейшие rollout используют локально собранный JAR без Gradle на VM.
- **2026-08-05 (production observability / Yandex Managed Prometheus):** Создан отдельный MSP workspace `mon9v9q5apml8dnnchl7`; на `vyasma-mafia` установлен и включен pinned Unified Agent `26.07.11`, который через metadata IAM собирает loopback Actuator и ограниченный набор Linux-метрик. В steady state отправляется 262 application + 33 system series. Загружены PromQL-алерты на пропажу метрик, долю 5xx и saturation HikariCP; critical маршрутизируется в Cloud Push + `polemica-fantasy-critical-email`, warning — в Cloud Push. Синтетический firing/resolved тест дал Success по обоим каналам и удалён; Telegram отложен до активации канала получателем.
- **2026-07-22 (production infra / Telegram egress):** Исходящий Bot API трафик production backend восстановлен после массовых TCP timeout к Telegram из Yandex Cloud. На fantasy VPS поднят отдельный `wg-tg` (`10.10.0.4`) до существующего входного VPS `51.250.97.185`, который уже выходит через зарубежный `91.108.240.119`; policy routing отправляет в туннель только официальные Telegram IPv4 CIDR из Docker-сети `172.23.0.0/16`, остальной egress сохраняет исходный адрес `51.250.18.236`. Туннель включен в systemd autostart; после restart десять root probes и авторизованный `getMe` прошли, новых `code=0 / Failed to call Telegram sendMessage` после cutover нет.
- **2026-07-20 (TMA / premium trophy cards):** Рейтинговые trophy cards в коллекции получили отдельный premium visual class: прозрачная conic metal frame вместо скрытого градиента под сплошным border, tier-specific champion/silver/bronze/finalist metal, glow/sweep/name shimmer, усиленный cap и компактную medal-плашку с местом и названием периода. Технический serial убран с лицевой стороны и остаётся в detail provenance; оформление работает и в grouped-player view, а animations отключаются через `prefers-reduced-motion`.
- **2026-07-20 (backend+TMA / finalist reward options):** Награды за 4–5 места больше не получают одинаковые первые три строки `fantasy_player`: варианты берутся из актуального RARE-пула активных auto-generated packs с учётом `excluded_from_pack_pool`, детерминированно перемешиваются по периоду и разнесены между reward recipients. Flyway **V73** пересобирает незавершённые bundled snapshots первого периода и сбрасывает несовместимые drafts; TMA для `BUNDLED_OPTIONS` показывает три готовые комбинации без ложного глобального поиска.
- **2026-07-19 (backend+admin / series player results audit):** Страница серии в admin получила lazy drawer **Player results** с матрицей текущих raw base points Polemica: `STANDALONE` нумеруется по хронологическому порядку, `POLEMICA_COMPETITION` — по сохранённому `game.num`; строки фактических участников сортируются по сумме, missing/unfinished/partial данные явно отмечаются, а строки копируются как TSV `Ник\tБалл1...`. Новый `GET /api/v1/admin/series/{id}/results` строит матрицу из `series_game.game_data_cache` и строгого parser публичной match-page без перков/rarity и без HTTP внутри DB-транзакции; физические дубли игр и игроков не скрываются.
- **2026-07-18 (DX / series announcements skill expansion):** `.codex/skills/polemica-create-series-from-announcement` расширен с ЛП/ЗЛ на любые активные `STANDALONE` и `POLEMICA_COMPETITION` турниры. Workflow теперь явно разделяет payload-поля по kind, поддерживает несколько серий из одного анонса, роли/замены/аннотации ростера, разрешение времени и номера по production-истории, duplicate/resume preflight и физическую DB-проверку `series_game`; примеры исправлены на обязательный порядок `--execute` перед subcommand.
- **2026-07-17 (launch / first periodic rating):** Flyway **V72** открывает первый короткий MAIN-период с 17 июля 00:00 до 20 июля 00:00 МСК (`[start, end)`) и добавляет в `/whats-new` анонс «Первый рейтинг периодов уже начался» с правилами top-10, достижениями до 2 500₣ и CTA `/rating`. Существующий открытый период с тем же стартом нормализуется вместо создания дубля.
- **2026-07-17 (backend+TMA / periodic rating achievements):** Добавлена категория достижений «Рейтинг периодов»: первый/пятый итоговый период, первый/пятый top-10, первый пьедестал и первая победа. Прогресс считается только по immutable entries финализированных периодов после launch cutoff, учитывает competition ties и пересчитывается targeted best-effort async-событием после finalize; lazy catalog/claim остаётся fallback. После economy review награды подняты до 50/250₣ за участие, 200/1 000₣ за top-10, 400₣ за пьедестал и 600₣ за победу (2 500₣ за всю линейку), без дублирования trophy-карт и скинов.
- **2026-07-17 (implemented / periodic rating rewards):** Реализован pilot biweekly MAIN-рейтинг по сумме `FantasyTeam.totalScore` финализированных серий, попавших в период по `MAX(series_game.played_at)`. Добавлены admin preview/finalize, competition ranks с наградами всем `rank <= 10`, top-10 trophy ladder, атомарная выдача карт и 50₣, reward draft/review/issue, серверный поиск игроков, реалистичный TMA preview, 12 trophy skins и immutable provenance на экземпляре `user_card`. Trophy-карты запрещены как merge inputs, но сохраняют обычный путь in-place Legendary upgrade. Scheduler/Telegram reminders и rollout feature flag оставлены отдельным этапом.
- **2026-07-17 (TMA / periodic rating archive + auto-issue):** `GET /api/v1/periodic-ratings/periods` отдаёт все `OPEN` / `SETTLING` / `FINALIZED` периоды с агрегатами; `/rating` получил селектор текущего и прошлых периодов. Для `FINALIZED` таблица и личные серии читаются из immutable snapshot. Финальное подтверждение награды пользователем теперь атомарно создаёт `user_card` и сразу переводит entitlement в `FULFILLED`; `REVIEW_REQUIRED` и ручной approve сохранены только для legacy/exception записей.
- **2026-07-16 (design / periodic rating awards):** После уточнения продукта discovery исправлен в `docs/features/DESIGN-PERIODIC-RATING-AWARDS.md`: рейтинг периода суммирует существующие `FantasyTeam.totalScore` пользователя по финализированным сериям выбранной лиги, а не очки отдельных `user_card`. Рекомендуемый pilot — biweekly MAIN, серия целиком относится к периоду по последней `series_game.played_at`, результат фиксируется immutable snapshot; уникальная EPIC остаётся наградой победителю.
- **2026-07-11 (backend / onboarding incident):** Исправлен production `500` в `/api/v1/onboarding/checklist`: onboarding больше не запрашивает единственную `fantasy_team` пользователя на серию, что несовместимо с MAIN + BUDGET, а проверяет наличие хотя бы одной команды через repository `exists`. Production root cause: `NonUniqueResultException` при двух командах пользователя в одной серии. Проверка: `./gradlew compileKotlin compileTestKotlin`.
- **2026-07-09 (backend / perk balance):** Flyway **V68** обновляет системные `bonus_points` перков по результатам production-статистики и жалоб игроков: `sniper` 5 → 4, `firstKickedFullGuess` 3.25 → 3, `winThreeToThree` 1.5 → 1.3, `ninja` 1.3 → 2. Миграция также публикует release note “Баланс перков обновлен” в `/whats-new`; пересчета уже завершенных результатов не запускает.
- **2026-07-03 (DX / Telegram support export):** Добавлен локальный `scripts/telegram-support-export/export_support_messages.py` на Telethon для read-only выгрузки истории support-супергруппы через user-account MTProto. Скрипт фильтрует forwarded user messages по умолчанию, пишет JSONL/CSV и первичный Markdown-разбор на bug/feature/question/noise; credentials/session/out закрыты локальным `.gitignore`.
- **2026-07-03 (backend / admin fantiki notifications):** Ручное начисление фантиков через admin `give-fantiki` теперь публикует `AdminFantikiGrantNotificationEvent` после коммита транзакции. Listener отправляет пользователю plaintext Telegram-сообщение в категории `ADMIN_BROADCAST` («Сообщения от администрации») с суммой начисления, балансом после операции и `adminReason`.
- **2026-07-02 (design / profile cosmetics):** Зафиксирован discovery для применяемой профильной косметики в `docs/features/DESIGN-PROFILE-COSMETICS.md`. Решение: довести существующий `COSMETIC_UNLOCK` от claim/unlock до выбора в `/profile-customization` и публичного отображения; `PROFILE_FRAME` остается рабочим отдельным flow, `BADGE_STYLE` в V1 остается через featured achievements. Предложен Flyway **V67**: каталог `profile_cosmetic`, поля `profile_title_code` / `profile_accent_code` / `profile_background_code` в `user_profile_customization`, seed из существующих achievement reward codes, backend/TMA contract и acceptance criteria.
- **2026-07-01 (DX / multi-agent feature delivery):** Добавлен project skill `.codex/skills/polemica-feature-delivery` для реализации крупных фич через delivery-loop: main agent как integration owner, plan writer + plan reviewer gate, pre-code QA/risk reviewer, vertical-slice worker до стабилизации API contract, ownership fan-out после стабилизации, code reviewer gate, local tester и UX reviewer до/после реализации. `polemica-feature-discovery` теперь явно передаёт крупные фичи в delivery skill после discovery.
- **2026-07-01 (TMA / achievements UX):** `/achievements` получил primary-фильтры по состоянию награды: **Забрать**, **Не завершено**, **Получено**. По умолчанию открывается `Забрать`, если есть `COMPLETED_UNCLAIMED`, иначе `Не завершено`; pending `CARD_CHOICE_ROLL` остаётся в actionable-потоке через resume CTA **Выбрать**. Внутри вкладок достижения группируются по категориям, `IN_PROGRESS` показывается перед `LOCKED`, а подпись `LOCKED` заменена на «Не начато». Проверки: `npm run build` в TMA, Browser QA на `/achievements` desktop/mobile, переключение фильтров, pending card-choice resume и отсутствие console warn/error.
- **2026-06-30 (backend+admin+TMA / stream links):** Серии и турниры получили списки ссылок на трансляции: Flyway **V66** добавляет `tournament_stream_link` и `series_stream_link` (`label`, `url`, `display_order`), admin create/edit турнира и серии редактирует ссылки списком. User API отдаёт effective `streamLinks` для серии как ссылки турнира + ссылки серии; добавлен `GET /api/v1/tournaments/active-series` для главной TMA, чтобы активные серии (`ACTIVE`/`SCORING`) оставались видимыми независимо от дедлайна состава. TMA показывает ссылки на главной и странице серии; `UPCOMING` скрывает трансляции, главная использует icon-only кнопки, а страница серии — иконка + подпись. Проверка: `./scripts/codex-check.sh quick`, затем targeted backend/TMA build прошли; Vite сохранил существующий warning по крупному `antd-vendor`.
- **2026-06-30 (backend / sheriff perks):** Добавлены два перка скоринга: `sheriffCheckBlack` («Проверка черного», `SHERIFF`, `MULTIPLE_PER_GAME`, +0.75 за каждого уникального проверенного черного/дона) и `voteOutSheriffDay1Or2` («Снять шерифа», `DON`/`MAFIA`, `ONCE_PER_GAME`, +2.75 если шериф ушел голосованием на 1 или 2 день). Flyway **V65** сидит каталог и applicable roles, а также выключает `ninja` из random cards (`can_appear_on_random_cards = false`) по жалобам игроков; уже выданные карты не меняются. Release note для `/whats-new` не сидится миграцией: пользователь напишет его вручную. `scripts/balance_perks.py` знает новые роли. Проверки: backend `compileKotlin compileTestKotlin` и targeted `StandardPerkDetectorsTest` / `PerkDetectorRegistryTest` прошли. Оценка бонусов сделана read-only sample-расчетом по production profiles: 1000 из 18170 уникальных матчей, target excluding `ninja` = 0.0618; полный `/perk-statistics/collect` оказался слишком тяжелым для интерактивного запуска.
- **2026-06-29 (backend+admin / fantasy player aliases):** `fantasy_player_alias` (Flyway **V64**) стал source of truth для Polemica id aliases; `fantasy_player.polemica_user_id` оставлен как cached primary alias. Admin Players показывает aliases, умеет добавлять alias и merge source player into target через preview/confirm с blockers для pending JSON/config references, replacement conflicts и active-team duplicate-player conflicts. Add player by Polemica id, STANDALONE sync, scoring, perk statistics и replacement validation учитывают aliases; merge переносит прямые FK на target, `card_template` без dedupe, а `user_card` сохраняются через шаблоны.
- **2026-06-28 (backend+admin / audit фантиков):** Ручные операции админки `Grant fantiki` / `Take fantiki` теперь требуют текстовую причину и сохраняют её в `fantiki_transaction.admin_reason` (Flyway **V63**). Admin API получил журнал операций `GET /api/v1/admin/users/fantiki-transactions` с опциональными фильтрами `telegramUserId` и `reason`; админка по умолчанию показывает админские начисления `ADMIN_GRANT` по всем пользователям, но позволяет переключиться на списания или все транзакции. Автоматические операции экономики продолжают писать только enum `reason`.
- **2026-06-26 (DX / TMA CSS modularization):** `polemica-fantasy-webapp/src/index.css` больше не хранит весь TMA stylesheet монолитом; он оставлен как ordered import manifest, а правила механически разнесены в `src/styles/*.css` по доменам (`base`, `layout`, `collection`, `card-merge`, `pack-opening`, `marketplace`, `profile`, `card-skins`, `achievements` и др.) без переименования классов и без изменения порядка каскада. Проверки: `git diff --check` для CSS и `npm run build` в `polemica-fantasy-webapp` — успешно.
- **2026-06-26 (backend+TMA / favorite player badge choice):** Для достижения `same_player_3_rarities` / бейджа «Любимый игрок» добавлен пользовательский выбор конкретного `fantasy_player` среди игроков, по которым у пользователя есть активные карты во всех требуемых редкостях. Flyway **V62** хранит выбор в `user_profile_customization.favorite_badge_fantasy_player_id`; backend валидирует выбор по тем же условиям достижения и использует его в `/achievements`, `/me/profile-customization` и публичной витрине профиля; TMA `/profile-customization` показывает селект «Любимый игрок».
- **2026-06-25 (backend+admin+TMA / card merge):** Реализовано слияние карт по `docs/features/DESIGN-CARD-MERGE.md`: Flyway **V61** добавляет audit/preview tables для merge, backend отдаёт user API `GET /api/v1/cards/merge/options`, `POST /api/v1/cards/merge/preview`, `POST /api/v1/cards/merge/confirm`, admin read-only API `/api/v1/admin/card-merges`, а TMA получила страницу `/cards/merge` с выбором игрока, материалов, скина, перков и подтверждением irreversible операции. Входные карты soft-delete'ятся, результат создаётся как новый `user_card` с `CARD_MERGE`, операции учитываются в achievements; marketplace create дополнительно лочит карту, чтобы ACTIVE-listing не создавался на расходуемую карту. Admin получил страницу **Card merges**, `/help` и release note объясняют механику. Проверки: backend `compileKotlin compileTestKotlin`, `CardMergeServiceTest`, TMA/admin `npm run build`, `./scripts/codex-check.sh quick` прошли; локальный smoke повторно прогнан на backend `28080/28081` из-за занятого `8080`, включая API preview, TMA `/cards/merge` до preview-state и admin `/card-merges`.
- **2026-06-25 (backend+TMA / legendary upgrade veteran discount):** Legendary upgrade теперь использует скидку за переподписание контракта: effective cost = `legendary.upgrade.cost` со снижением на `marketplace.contract_reissue_discount_percent × times_renewed` (текущие цены `↻0/1/2`: 400/340/280₣). Backend списывает effective cost конкретной `user_card`, `/legendary-upgrade/info` отдаёт `costTiers`, TMA wizard и `/help` показывают цену по контракту, Flyway **V60** публикует release note в `/whats-new`.
- **2026-06-25 (design / card merge):** Зафиксирован draft-дизайн механики слияния карт в `docs/features/DESIGN-CARD-MERGE.md`: V1 поддерживает `3 COMMON -> 1 RARE` и `3 RARE -> 1 EPIC` только для одного `fantasy_player`; `RARE -> EPIC` берёт 2 уникальных перка из входных RARE, а при `A/A/A` сохраняет `A` и даёт выбрать второй перк из roll без `A`. Результат создаётся новым `user_card`, входные карты soft-delete, `times_renewed = max(inputs)`, `uses_remaining = min(baseUses(resultRarity), sum(inputUses))`, история сохраняется через `CARD_MERGE` и отдельные audit tables. UX уточнён: roll фиксируется для выбранного набора материалов без free reroll; TMA показывает полноценный preview, value loss, BUDGET пригодность, потерю скинов и CTA `Снять с продажи` для ACTIVE marketplace cards; `/help` и `/whats-new` получают объяснение фичи; V1 seed включает concrete merge achievements.
- **2026-06-25 (backend+admin+TMA / public series number):** Серии получили отдельный `public_number` для пользовательского бейджа «Серия N». Значение вычисляется на backend из последнего числа в `series.name`, а если чисел нет — `1`; при изменении названия пересчитывается. `game_num_from/game_num_to` остаются только техническим диапазоном игр Polemica для sync. TMA использует `publicNumber` вместо `gameNumFrom` для бейджа, админка показывает номер в списке серий, но не требует ручного ввода.
- **2026-06-24 (backend+admin / fantasy player catalog):** В админку добавлена глобальная страница **Players** для `fantasy_player`: поиск по нику / fantasy id / Polemica id, создание игрока, редактирование ника, загрузка фото и быстрое добавление выбранного игрока в целевой турнир. Backend получил admin endpoints `GET/POST/PUT /api/v1/admin/fantasy-players` и `POST /api/v1/admin/fantasy-players/{id}/photo`; `POST /api/v1/admin/tournaments/{id}/players` теперь совместимо принимает либо старые `polemicaUserId + nickname`, либо `fantasyPlayerId`. Форма Add player на странице турнира умеет искать существующего игрока из каталога.
- **2026-06-24 (backend+admin / direct bot messages):** Админская страница Broadcast переименована в **Bot messages** и получила вкладку **Direct message**: поиск пользователя через `/api/v1/admin/users`, предпросмотр/валидация Telegram MarkdownV2 и отправка одному `telegram_user` через бота. Backend добавил `POST /api/v1/admin/notifications/direct`, который валидирует существование пользователя, использует `NotificationDeliveryService` с категорией `ADMIN_BROADCAST`, возвращает результат доставки (`sent/skippedBlocked/skippedPreference/failed`) и сохраняет прежнюю массовую рассылку без изменений. `AdminUserListItemDto` теперь отдаёт `botBlocked`, а Users показывает статус бота.
- **2026-06-16 (backend+admin+TMA / CHOOSE-паки):** Добавлен режим открытия паков `CHOOSE`: покупка создаёт pending choice из 3 заранее сохранённых вариантов без немедленной выдачи карт, а `POST /api/v1/store/pack-choices/{choiceId}/select` материализует выбранный вариант через общий pack-opening pipeline. Flyway **V58** добавляет `card_pack.open_mode` и `user_card_pack_choice` с reservation/payment fields и partial unique pending index. TMA store показывает “Выбор 1 из 3 наборов”, pending resume CTA и горизонтальный scroll-snap overlay с компактными картами; после выбора запускается существующий `PackOpening`. Admin Card packs получил режим Instant/Choose, CHOOSE принудительно auto-generated; User tools/admin open-pack не открывают CHOOSE напрямую.
- **2026-06-16 (backend+admin / series games manual management):** В админке серии добавлен блок **Games**: список `series_game` с Polemica id, display name, num/table/phase, playedAt и scored/finished статусами; add по Polemica game id и delete с подтверждением. Backend получил admin endpoints `GET/POST/DELETE /api/v1/admin/series/{id}/games`; add для `POLEMICA_COMPETITION` принимает только игры из связанного соревнования, не пересчитывает scores автоматически и помечает строку `scored=false`; delete запрещен для finalized серий, удаляет per-game score breakdown этой игры и пересобирает `fantasy_team_card.score` / `fantasy_team.total_score` из оставшихся breakdown-строк.
- **2026-06-14 (DX / series announcements skill):** Добавлен проектный skill `.codex/skills/polemica-create-series-from-announcement` для создания production-серий по Telegram/text/image-анонсам `Лиги Претендентов` и `Закрытой лиги`. Workflow: распарсить лигу/номер/дату/время/ростер, read-only найти активный `STANDALONE` tournament и `tournament_player.id`, создать серию через admin API только в `UPCOMING`, назначить игроков и проверить результат read-only запросом. В skill добавлен helper `scripts/prod-admin-series.py`: dry-run по умолчанию, `--execute` для production write после approval, app API порт `18080` (не management `18081`).
- **2026-06-12 (TMA / no-photo player fallback):** В пользовательском приложении добавлен единый `PlayerImage` fallback для игроков без фото или с битой ссылкой: сначала используется прежнее правило `playerPhotoUrl ?? imageUrl`, а при `null`/`onError` рисуется CSS-силуэт со стабильным тоном по `fantasyPlayerId`. Компонент применён на карточках коллекции, сборке команды, pack opening, достижениях, marketplace, истории, лидербордах, сравнении, модалках и списках участников. Локально через `polemica-local-testing` поднят backend+admin+TMA; в Docker Postgres добавлены тестовые игроки `NoPhoto Alpha/Bravo/Charlie/Delta` без `photo_url`, привязанные к турниру `1` и серии `2`, плюс их тестовые карточки для пользователя `telegram_id=889524`; browser-проверка подтвердила fallback на `/tournaments/1/participants` и `/cards?view=players`.
- **2026-06-08 (backend+admin / series rewards top100):** Экономика наград серии получила новый tier `series.reward.top100` для 51–100 места. Flyway **V57** сидит `series.reward.top100=40`, переводит participation на 101+ место со значением `30` и поднимает `league.reward_scale.BUDGET` до `75%`. `EconomyConfigService.getSeriesReward` теперь различает 26–50, 51–100 и 101+; admin Economy сортирует `top100` между `top50` и participation, а TMA `/help` получает новый tier через `economy-info`.
- **2026-06-08 (backend+admin / tournament HTML reports):** В админке турнира добавлена генерация standalone HTML-репорта по явно выбранным сериям. Backend endpoint `GET /api/v1/admin/tournaments/{id}/report.html?seriesIds=...` строит отчет на лету из текущей БД, возвращает `text/html`, требует непустой список серий и валидирует принадлежность турниру. В отчете есть вкладки для обложки, лидеров лиг, рекордов, статистики, серий и топов карточек; топ карточек дедуплицируется по `card_template.fantasy_player_id`, а перки берутся из полного шаблона `card_template_perk` + `perk`. Admin `TournamentDetailPage` открывает модалку выбора серий и загружает HTML через Basic Auth `apiFetch` в Blob-окно.
- **2026-06-07 (backend+TMA / global card uses reservations):** Контракт карты теперь учитывает глобальный резерв зарядов по всем незавершённым сериям: submit команды лочит выбранные `user_card` и запрещает ставить карту, если `COUNT(DISTINCT series_league_id)` по `series.finalized = false` уже исчерпал `uses_remaining`. `/me/cards?seriesId` отдаёт `canJoinMoreLeagues` с учётом глобального резерва, а финализация серии больше не делает silent clamp до 0: overcommit возвращает `409 CONFLICT` с диагностикой карт. TMA `TeamPage` инвалидирует cards-cache после submit/update команды.
- **2026-06-02 (admin / marketplace complaint sanctions):** В модалке санкции сделки дефолтная `Награда жалобщику` теперь рассчитывается как доля от штрафа продавцу (`sellerFine / complaintsCount`), а не из комиссии marketplace. Warning в админке сравнивает суммарную награду жалобщикам со штрафом продавцу; комиссия остаётся информационной строкой.
- **2026-06-02 (backend+admin+TMA / marketplace product communication):** Flyway **V55** добавляет CTA-поля `button_text`/`button_url` для `release_note`, публикует лорный анонс переподписания контрактов в `/whats-new` со ссылкой на `/marketplace` и сидит draft-кампанию `Marketplace: переподписание контрактов` для Product communication. Формулировка: игрок переподписывает свой контракт, а после лимита и расхода энергии устает от мафии и уходит на покой. Admin Product comms теперь умеет задавать CTA для release notes и вручную отправлять draft-кампании; TMA `/whats-new` показывает CTA и трекает клик как `FEATURE_USED`.
- **2026-06-02 (backend+TMA / marketplace economy tuning):** Flyway **V56** обновляет экономику marketplace под релиз переподписаний: комиссия `marketplace.commission_percent = 15`, минимумы листинга COMMON `20₣`, RARE `40₣`, EPIC `120₣`; LEGENDARY остаётся `250₣`. Лорный анонс V55 и TMA `/help` дополняют это как повышение комиссии за сопровождение переподписаний и снижение порога входа для младших карт.
- **2026-06-01 (backend+TMA / public team deep-link startup):** Ускорена загрузка share/deep link команды (`startapp=team_*`): публичные `GET /series/{id}/users/{telegramId}/fantasy-team` и `/details` больше не запускают `pruneInvalidCardsForSeries` на read-path, а работают как read-only; pruning остаётся на изменении ростера серии в админке. `PublicFantasyTeamDto` теперь отдаёт `seriesName`, поэтому TMA-страница чужой команды убрала стартовый запрос полного `/series/{id}` и откладывает leaderboard/details до первичного рендера команды.
- **2026-05-31 (backend+TMA / marketplace contract reissues):** Marketplace-покупка теперь считается перезаключением контракта: `times_renewed` больше не сбрасывается при покупке, а увеличивается на 1 вместе с восстановлением `uses_remaining`; create/update/buy запрещены при достижении `renewal.max_times`. Flyway **V54** добавляет `marketplace.contract_reissue_discount_percent=15`, сбрасывает `times_renewed=0` у карт в ACTIVE-листингах на релизе и расширяет marketplace watch-фильтры диапазоном `times_renewed`. User marketplace DTO отдают `timesRenewed/maxRenewals/minListingPrice`; TMA показывает маркер `↻ N/max`, фильтрует лоты/отслеживания по контракту и использует effective min price для продажи/смены цены.
- **2026-05-30 (backend+admin+TMA / series player scoring replacements):** Для `series_player` добавлен optional `replacement_polemica_user_id`; админка позволяет задать raw Polemica id замены для выбранного игрока серии. Скоринг теперь считает карту по основному игроку, а если его нет в конкретной игре — по замене; в per-game breakdown сохраняется фактический scorer и TMA показывает строку «Замена». Изменение замен требует ручного `Calculate scores`.
- **2026-05-29 (admin / production blank screen):** Production admin showed a blank screen because the Vite 8 / Rolldown `maxSize` split cut Ant Design into multiple interdependent `antd-vendor-*` chunks, causing a runtime circular dependency (`Cannot read properties of undefined (reading 'Modal')`). Admin Vite config now keeps `antd` in one chunk and splits only `@rc-component` / `rc-*` support packages separately; local production preview renders the login screen again.
- **2026-05-28 (DX / Vite chunk warnings):** TMA and admin Vite configs now use Vite 8 / Rolldown `build.rolldownOptions.output.codeSplitting` groups to split React, TanStack Query, Telegram SDK, Ant Design/icons, and remaining vendor modules into smaller chunks. `npm run build` for both frontends passes without the previous >500 kB JS chunk warning.
- **2026-05-28 (TMA+backend / tournament archive):** На главной странице турниров добавлен блок «Архив турниров» для прошедших `FINISHED` турниров. User API получил `GET /api/v1/tournaments/archive`, возвращающий архив отдельно от активного списка; TMA показывает архив в конце страницы со ссылками на существующие страницы деталей турнира.
- **2026-05-27 (infra / prod compose healthcheck + DB backup):** `docker-compose.prod.yml` now gives `fantasy-db` and `fantasy-backend` `restart: unless-stopped`; prod backend has a Docker healthcheck against `http://localhost:8081/actuator/health` with 60s start period. Backend runtime image installs `curl` for that healthcheck while preserving the current fast VPS-side `build:` deploy flow. Added `scripts/prod-db-backup.sh` for host-level PostgreSQL custom-format dumps from the `fantasy-db` container into `$HOME/polemica-fantasy-backups/postgres` with retention.
- **2026-05-26 (DX / multi-agent feature discovery):** Добавлен workflow для работы с крупными фичами через Codex sub-agents: `docs/codex/MULTI_AGENT_WORKFLOW.md` фиксирует этапы intake → product discovery → design discovery → technical discovery → implementation, шаблоны product/TMA/admin/backend/QA агентов, правила ownership и integration. Добавлен проектный skill `.codex/skills/polemica-feature-discovery` для запуска spec-first product/design discovery перед кодингом.
- **2026-05-26 (backend / favorite-player achievement):** Flyway **V52** переводит `same_player_3_rarities` на условие `SAME_PLAYER_4_RARITIES`, описание “во всех 4 редкостях” и награду `BADGE_STYLE favorite_player` + `CARD_CHOICE_ROLL` RARE 2 из 5. Backend progress теперь требует 4 редкости, а `/achievements`, profile customization и public profile showcase показывают динамический title `Любимый игрок: {ник игрока}` при найденном qualified player.
- **2026-05-26 (TMA+backend / achievement card choice skins V1):** `/achievements` pending card choice UI переведён с raw images на компактные selectable cards на базе pack-opening summary card structure: rarity/skin classes, `aria-pressed`, selected check marker, responsive 92–112px grid and disabled submit until exact required count. Flyway **V51** сидит achievement card skins `budget_edition`, `common_challenge_edition`, `winner_edition`, `crafter_edition`, `pack_hunter_edition` and attaches them through `metadata.skinCode` to supported `CARD_CHOICE_ROLL` rewards; `top_quarter_10` is a temporary common-challenge preview until a real common-only condition exists. Targeted backend seed/lifecycle tests and TMA build passed.
- **2026-05-26 (backend / achievement card rewards foundation):** Добавлен backend foundation для карточных наград достижений: user DTO теперь несут `metadata` и card reward claim result structures, backend validation принимает `RANDOM_CARD` / `CARD_CHOICE_ROLL` только с валидным JSON metadata и больше не разрешает `CARD_SKIN_UNLOCK` как cosmetic reward. Flyway **V49** добавляет `user_achievement_card_choice` для pending choice state и расширяет ownership history check constraint значением `ACHIEVEMENT_REWARD`, без seed-rework. `CardPackService` exposes active auto-generated pack eligible player pool by rarity, а `AchievementCardRewardService` умеет формировать options из active packs и отсекает `LEGENDARY` для pack-source rewards.
- **2026-05-25 (design / achievement card rewards):** Зафиксировано future-решение для карточных наград достижений: rewards могут выдавать случайные карты, несколько карт, выбор из roll или выбор игрока из каталога. `RANDOM_CARD` и `CARD_CHOICE_ROLL` выбирают eligible `fantasy_player` из правил активных паков; `CARD_CHOICE_CATALOG` позволяет выбрать любого `fantasy_player` из каталога. Доменный инвариант: любой игровой путь, создающий `user_card`, должен уметь создавать новый `card_template`, если подходящего template еще нет; это распространяется на паки, achievement rewards, специальные события и будущие источники карт. Achievement-edition стиль должен применяться через `user_card.card_skin_id`, а отдельного `CARD_SKIN_UNLOCK` как пользовательской награды быть не должно. Длинные цепочки расширяются новыми уровнями в существующих `chain_group` (например 50/100/150), не изменяя уже выданные пороги. Черновая таблица переработки наград вынесена в `docs/features/DESIGN-ACHIEVEMENTS-REWARD-REWORK.md`; в ней убран столбец старых наград, добавлены production retro completion counts, разделы новых challenge и secret/easter egg achievements, а также rebalance наград по частотности `Ретро игроков`. Последнее решение: любая косметическая награда (`BADGE_STYLE`, `PROFILE_FRAME`, `PROFILE_COSMETIC`) должна идти с реальным довеском, чаще всего `CARD_CHOICE_ROLL`; средние/сложные награды чаще используют формат 2 из 5 и местами подняты на тир редкости. `series_win_50` подтвержден как aspirational `PROFILE_FRAME` + `CARD_CHOICE_ROLL` 1 из 3 LEGENDARY. Достижение "получить LEGENDARY из пака" исключено, потому что LEGENDARY не выпадает из паков; часть no-LEGENDARY/no-EPIC challenge-линий ограничена MAIN-лигой.
- **2026-05-25 (backend+TMA / future achievements enabled):** Включены все ранее dormant достижения marketplace/social/legendary-upgrade. Backend: Flyway **V48** включает 12 definitions для существующих DB и добавляет `user_legendary_upgrade_event`; fresh seed V46 теперь сразу включает все 42 достижения. Progress evaluators считают post-launch SOLD-сделки без sanctions, watch-фильтры, product events `SHARE_PROFILE`/`SHARE_TEAM`/`COMPARE_OPEN`/`PUBLIC_PROFILE_VIEW` и timestamped legendary upgrades. События прогресса публикуются из покупки marketplace, создания watch-фильтра, product events и legendary upgrade. TMA отправляет product events из share profile/team/place, compare views и public profile view.
- **2026-05-25 (backend+TMA / achievement profile frames in public lists):** Profile frame cosmetic selection is now exposed through public user snippets for global rating and series/tournament leaderboards via `user.profileFrameCode`, resolved only when the selected frame is still unlocked. TMA renders the selected frame as a compact inline outline around player names in rating and leaderboard rows, including pinned rows; top bar and private work screens remain unchanged.
- **2026-05-25 (backend+admin / user achievements Stage 3 admin):** Added admin achievements management: `GET /api/v1/admin/achievements`, `PATCH /api/v1/admin/achievements/{code}` with metadata/flags/full reward replacement, preserved the first-enable `tracking_started_at` invariant and claimed reward snapshots, and added admin `/achievements` UI with persisted analytics and dry-run. No marketplace/social/legendary evaluators, event sources, listeners, schedulers, or TMA achievement changes were added in Stage 3. Verification: Stage 3 targeted test, Stage 1/2 achievement regression tests, backend compile, admin build, and `./scripts/codex-check.sh quick` passed; local Browser smoke reached admin login but could not authenticate against the already-running backend with available local credentials.
- **2026-05-25 (backend+TMA / user achievements Stage 1):** Реализован первый вертикальный срез пользовательских достижений. Backend: Flyway **V46** добавил `achievement_definition`, `achievement_reward`, `user_achievement`, `user_cosmetic_unlock`, timestamped `user_card_pack_open_event`, `fantasy_team.created_at` и `series.finalized_at`; seed содержит 42 основных определения, из них 30 enabled/visible Stage 1 и 12 dormant disabled с `tracking_started_at = NULL`, все с `history_policy = FROM_ACHIEVEMENTS_LAUNCH`. Добавлены user API `GET /api/v1/achievements`, `POST /api/v1/achievements/{code}/claim`, admin dry-run `POST /api/v1/admin/achievements/backfill/dry-run`; прогресс пересчитывается post-commit через `AchievementProgressEvent` + `@TransactionalEventListener(AFTER_COMMIT)`, claim делает lock/upsert `user_achievement` и выдает фантики через `ACHIEVEMENT_REWARD`. Stage 1 evaluators включают participation, BUDGET, results, collection и packs; marketplace/social/legendary-upgrade definitions остаются dormant. TMA: добавлена минимальная страница `/achievements` со списком, прогрессом и claim; после claim инвалидируются `['achievements']` и `['me']`. Проверки: targeted `AchievementStage1IntegrationTest` — успешно, backend `compileKotlin compileTestKotlin` — успешно, TMA/Admin `npm run build` — успешно, `./scripts/codex-check.sh quick` — успешно.
- **2026-05-25 (design / user achievements):** Согласован и добавлен дизайн-док `docs/features/DESIGN-ACHIEVEMENTS.md` для новой системы пользовательских достижений. V1: автоматические системные достижения с витриной в публичном профиле, гибридный каталог (условия в коде, тексты/иконки/награды/видимость в админке), claim-based награды и launch baseline без выдачи за старые действия. Seed-каталог уточнен до 42 основных достижений + 2 optional secret; все seed-достижения должны иметь `history_policy = FROM_ACHIEVEMENTS_LAUNCH`. `CURRENT_STATE` и `RETROACTIVE_CUMULATIVE` оставлены только как future-only технические policy после отдельной оценки экономики. Production dry-run snapshot на 2026-05-25 показал, что rejected retroactive/current-state вариант дал бы potential launch liability **343 695₣**; финальное решение V1 — dry-run перед релизом должен подтвердить **0₣** instant payout. Награды ограничены экономикой и косметикой: небольшие фантики, бейджи, профильные рамки, cosmetic/card-skin unlock; прямые бонусы к скорингу, силе карт, uses или правилам лиг запрещены. `.superpowers/` добавлен в `.gitignore` как рабочий артефакт visual companion.
- **2026-05-24 (backend+TMA+admin / rename achievements to perks):** Терминология проекта переименована с achievements/достижений на perks/перки в backend, TMA, admin, API-контрактах и документации. Backend: сущности/репозитории/DTO/сервисы/контроллеры переехали на `Perk*`, scoring package — `scoring/perk`, user/admin endpoints — `/api/v1/perks`, `/api/v1/admin/perks`, `/api/v1/admin/perk-statistics`, card-template endpoint — `/card-templates/{id}/perks`, query/body поля — `perkIds`/`perkId`. DB: Flyway **V45** переименовывает `achievement` → `perk`, `card_template_achievement` → `card_template_perk`, `fantasy_team_card_game_achievement` → `fantasy_team_card_game_perk`, `card_pack_achievement` → `card_pack_perk`, `marketplace_watch_filter_achievement` → `marketplace_watch_filter_perk`, `achievement_bonus` → `perk_bonus`, `achievement_ids_key` → `perk_ids_key`, а также economy key `card.value.achievement_bonus` → `card.value.perk_bonus`. Frontend: типы, API clients, страницы и UI-тексты переведены на перки; CSS chip-классы переименованы с `pf-card-ach-*` на `pf-card-perk-*`. Проверки: backend `compileKotlin compileTestKotlin`, `npm run build` для TMA и admin — успешно.
- **2026-05-24 (backend+TMA / perk filters for collection and marketplace):** Добавлен мультивыбор перков с OR-семантикой для TMA коллекции, маркетплейса и marketplace watch-фильтров. Backend: `GET /api/v1/me/cards` и `GET /api/v1/marketplace/listings` принимают повторяемый `perkIds`; watch DTO/request расширены перками; Flyway **V44** добавил `marketplace_watch_filter_perk` и нормализованный ключ дедупликации. Watch-уведомления теперь матчятся по пересечению перков карты и фильтра. TMA: фильтр перков добавлен на `/cards`, `/marketplace`, `/notifications/marketplace-watches`. Проверки: backend `compileKotlin compileTestKotlin`, targeted tests для filters/watch, `npm run build` (`polemica-fantasy-webapp`) — успешно.
- **2026-05-24 (DX / production DB readonly skill):** Добавлен проектный Codex skill `.codex/skills/polemica-prod-db-readonly` для безопасного чтения production PostgreSQL через VPS (`mafia@51.250.18.236`) и Docker Compose service `fantasy-db`. В skill включены guardrail’ы: без секретов в репозитории/чате, только read-only запросы, короткие таймауты, запрет psql meta-команд и очевидных write/maintenance SQL. Helper `scripts/prod-db-readonly.sh` оборачивает SQL в `BEGIN TRANSACTION READ ONLY ... ROLLBACK`.
- **2026-05-24 (TMA / player profile series wins):** В публичный профиль игрока добавлен агрегат побед в завершённых сериях по лигам: backend `GET /api/v1/players/{telegramId}/profile` возвращает `seriesWins` с total и разбивкой по leagueCode, TMA показывает «Победы в сериях» с основной и бюджетной лигой. Ник текущего пользователя в верхней панели TMA стал ссылкой на собственный профиль `/players/{telegramId}`. Проверки: `./gradlew compileKotlin` (`polemica-fantasy-backend`) и `npm run build` (`polemica-fantasy-webapp`) — успешно.
- **2026-05-24 (TMA / social share and compare):** Реализован P1 social layer без новых backend/privacy-моделей. TMA получил helper Telegram share links (`shareURL` / `openTelegramLink`, `startapp` parsing), deep-link routing из `start_param`, кнопки share для команды, места в серии, карточки в публичном контексте команды/серии и профиля пользователя. Добавлены compare views: `/series/:seriesId/compare/:telegramId?league=...` и `/tournaments/:tournamentId/compare/:telegramId?league=...` на существующих authenticated/public TMA API. Share/deep links используют `VITE_TMA_BOT_USERNAME` (fallback `VITE_TELEGRAM_BOT_USERNAME`) и optional `VITE_TMA_APP_SHORT_NAME`; для production host `fantasy.maftourbot.ru` есть runtime default `polemica_fantasy_bot` без short name. Если share-route открыт в браузере без Telegram initData, TMA редиректит его в Telegram deep link вместо показа ошибки initData. Проверка: `npm run build` (`polemica-fantasy-webapp`) — успешно; `npm run lint` остаётся красным на ранее существующих lint-нарушениях в `CardValueBadge`, `LegendaryUpgradeWizard`, `MarketplacePage`, `MarketplaceWatchesPage`.
- **2026-05-24 (memory-bank / glossary and operational insights):** Добавлены `memory-bank/glossary.md` с каноническими терминами проекта и `memory-bank/operationalInsights.md` с рабочими наблюдениями, включая ключевой same-day insight: серии часто появляются в сервисе в день проведения игр, поэтому UX/уведомления/sync не должны предполагать долгий период `UPCOMING`. `AGENTS.md` теперь указывает читать эти файлы для broad/product work.
- **2026-05-24 (backend+TMA+admin / anti-churn onboarding and product comms):** Реализован V1 анти-churn коммуникаций. Backend: Flyway **V43** добавил `onboarding_progress`, `release_note`/`release_note_view`, `product_campaign`, `product_event`; появились user API `/api/v1/onboarding`, `/api/v1/release-notes`, `/api/v1/product-events`, категория `ONBOARDING_TIPS`, `/start` 2.0 с inline-меню и state-based текстом, hourly onboarding nudges с лимитом 1/день и 3 lifetime, campaign/release/analytics endpoints в `/api/v1/admin/notifications`. TMA: чеклист на главной, `/whats-new` с badge, auto-complete шагов через реальные экраны/действия и campaign event tracking. Admin: страница **Product comms** для campaigns, release notes и analytics summary. Проверки: backend `compileKotlin compileTestKotlin`, targeted bot/menu tests, `npm run build` для TMA и admin — успешно.
- **2026-05-24 (TMA / TeamPage series overview link):** Локально проверен новый `polemica-local-testing` workflow: `./scripts/local-up.sh --generate-init-data` поднял backend+admin+TMA, TMA открылся с валидным dev initData. Для проверки создана локальная серия `Codex Local Test Series`; обнаружено, что ссылка **«Обзор серии»** на `TeamPage` вела обратно на `/series/{id}/team?league=MAIN`. Исправлено на `/series/{id}?league=MAIN`; browser-проверка подтвердила переход на настоящий `SeriesPage`. Проверка: `./scripts/codex-check.sh webapp` — успешно.
- **2026-05-24 (DX / local UI testing skill + TMA initData):** Добавлен генератор свежего Telegram Mini App initData `scripts/generate-tma-init-data.py`: берёт `TELEGRAM_BOT_TOKEN` из env или корневого `.env`, печатает `VITE_DEV_INIT_DATA` (или `--format raw/json`), поддерживает dev-пользователя через CLI-параметры. `scripts/local-up.sh` получил `--generate-init-data`, чтобы запускать backend+admin+TMA без ручного копирования токена. Старый helper-test `TelegramInitDataValidatorTest.printViteDevInitDataSample` помечен `@Disabled` и теперь указывает на скрипт. Добавлен проектный Codex skill `.codex/skills/polemica-local-testing`, описывающий локальный запуск, генерацию initData, Browser-проверки и целевые команды.
- **2026-05-24 (DX / Codex workflow):** Актуализирован агентский контекст для разработки через Codex: `AGENTS.md` получил раздел `Codex Workflow`, версию `polemica-library` обновили до **1.8.8**, диапазон Flyway-миграций до **V42+**, а список key tests синхронизирован с текущим деревом тестов. Добавлен `scripts/codex-check.sh` с целями `quick/backend/backend-test/webapp/admin/frontend/lint`, README дополнен разделом «Разработка через Codex», `memory-bank/techContext.md` синхронизирован с текущими версиями и проверочными командами.
- **2026-05-19 (backend / pair ban idempotency):** Уточнено поведение `POST /api/v1/admin/marketplace/ban-pair`: при бане пары карты больше не удаляются и не soft-delete'ятся (`user_card.deleted_at` не трогается), SOLD-листинги пары помечаются через `marketplace_listing_sanction` как уже учтённые санкцией. Повторный бан пары считает фантики и карточный payload только по SOLD-листингам без санкции, поэтому уже оштрафованные транзакции не списываются повторно; новые сделки пары после первого бана остаются санкционируемыми. `MarketplacePairBanFantikiIntegrationTest` расширен регрессиями на сохранение карт/листингов, идемпотентность повторного бана и повторный бан только новых транзакций. Проверки: `./gradlew compileTestKotlin` — успешно; целевой Testcontainers-тест не стартовал из-за недоступного Docker (`ContainerFetchException`).
- **2026-05-19 (backend+admin / pair trades createdAt and complaintsCount):** В Pair trades админки добавлено время создания листинга и количество жалоб на операцию. Backend: `PairTradeDto получил поля createdAt` (из `MarketplaceListing.createdAt`) и `complaintsCount` (агрегация через `MarketplaceComplaintRepository.countGroupedByListingIds`); в `MarketplaceAdminService.getPairTrades` добавлен batch-запрос жалоб для всех sold listings между юзерами. Admin UI (`MarketplaceModerationPage`): в таблице pair trades добавлена колонка «Created at» (время листинга, width=180) и «Жалобы» (число жалоб, красный тег если ≥3). TS-контракты синхронизированы (`api/types.ts`). Проверки: `./gradlew compileKotlin` + `npm run build` — успешно.
- **2026-05-16 (backend+admin / marketplace complaints market context):** Для админ-модалки санкции по жалобам добавлена аналитика рынка на момент выкупа. Backend: `ComplainedTransactionDto` получил `createdAt`, `TransactionComplaintsListDto` расширен `marketContext` (`listingCreatedAt`, `concurrentSameTemplate`, `concurrentSamePlayerRarity`), добавлен DTO `ConcurrentListingDto`; в `MarketplaceListingRepository` новый JPQL `findConcurrentListingsForContext` (same fantasy-player + same rarity, `createdAt <= soldAt`, ACTIVE now или SOLD после момента сделки, fallback по snapshot `soldCardTemplate`), в `MarketplaceAdminService` построение и сортировка контекста + проброс `createdAt` в список жалоб. DB: Flyway **V42** (`idx_marketplace_listing_user_card`) для ускорения lookup по `user_card_id` вне partial ACTIVE-индекса. Admin UI (`MarketplaceModerationPage`): колонка «Создан» в таблице complained-transactions, в модалке санкции поля «Создан» + «Время на рынке», новый блок «Рынок на момент выкупа» с 2 таблицами (те же перки / та же редкость) и цветовой маркировкой цены (`Дешевле` = orange). TS-контракты синхронизированы (`api/types.ts`).
- **2026-05-16 (backend+admin+debug / STANDALONE started-day filter):** Для серий `STANDALONE` добавлен опциональный фильтр по календарному дню `started` Polemica-игры: новая колонка `series.game_started_on` (Flyway **V41**, `DATE NULL`). Backend: поле `gameStartedOn` проброшено в `Series`/admin DTO и create/update request; в `UpdateSeriesRequest` добавлен `gameStartedOnSpecified` для явного сброса `null`. В `SeriesService` фильтр разрешён только для `STANDALONE` (для `POLEMICA_COMPETITION` не-null значение отклоняется 400 и хранится `null`). В `DefaultGameSyncService.fetchStandalonePrepared` добавлен фильтр `game.started.toLocalDate() == gameStartedOn` (если задан). Админка (`SeriesFormModal`, `SeriesDetailPage`) получила DatePicker без времени для `gameStartedOn` + clear/reset; в списке серий `TournamentDetailPage` дата показывается рядом с `Prefix`. Скрипт `scripts/trace_series_game_sync.py` синхронизирован: учитывает `gameStartedOn` и печатает статистику отсечения по started-дню. Проверки: `./gradlew compileKotlin compileTestKotlin`, `npm run build` (admin), `python3 -m py_compile scripts/trace_series_game_sync.py`; интеграционный запуск Testcontainers-теста упёрся в отсутствие Docker (`ContainerFetchException`).
- **2026-05-15 (TMA / anti-flicker for legendary + tournament_gold cards):** В `polemica-fantasy-webapp/src/index.css` оптимизированы анимации редких рамок для снижения мерцания в WebView/мобильных устройствах: из `@keyframes pf-legendary-crafted-glow` и `pf-skin-gold-glow` убран `filter` (оставлен pulsing `box-shadow`), добавлены compositing hints (`will-change: box-shadow`/`background-position`/`transform`) для glow/shimmer/sweep анимаций, а у `tournament_gold` sweep-псевдоэлемента уменьшен экстент (`inset: -60% -> -20%`, подправлены stop'ы градиента). Проверки: `npm run build` (`polemica-fantasy-webapp`) + lints для `src/index.css` — успешно.
- **2026-05-15 (TMA / tournament_gold visual uniqueness):** Обновлён стиль `tournament_gold` в `polemica-fantasy-webapp/src/index.css`, чтобы он меньше пересекался с `legendary-crafted`: палитра сдвинута с «чистого янтарного» в «шампань + изумруд» (рамка, glow, sweep, бейдж, gradient имени, perk chips/bonus). Логика скинов и API не менялись; затронут только visual слой.
- **2026-05-15 (local bootstrap script):** Добавлен единый скрипт локального старта `scripts/local-up.sh` для одновременного подъёма backend (`docker compose up -d fantasy-backend`) и dev-серверов `polemica-fantasy-admin` + `polemica-fantasy-webapp`. Скрипт поддерживает параметры `--init-data`, `--host`, `--admin-port`, `--tma-port` и ENV-аналоги (`VITE_DEV_INIT_DATA`, `DEV_HOST`, `ADMIN_PORT`, `TMA_PORT`), проверяет зависимости (`docker`, `npm`, `curl`), ждёт health backend на `:8081`, пишет логи фронтов в `.local-dev-logs/`, а по `Ctrl+C` останавливает только frontend-процессы (docker-контейнеры остаются запущенными). README дополнен разделом быстрого старта.
- **2026-05-15 (special card skins / tournament_gold):** Закрыт полный вертикальный срез по косметическим скинам карт. Backend: Flyway `V40` (`card_skin`, `user_card.card_skin_id`, `card_pack.card_skin_id`, `marketplace_listing.sold_skin_code`) + seed `tournament_gold`; добавлены `CardSkin`/`CardSkinRepository`; в `CardPackService.openPack` карты получают скин пака; в `MarketplaceService.buyCard` фиксируется `soldSkinCode`; `skinCode` проброшен в user/admin DTO (коллекция, маркетплейс, feed, transaction detail, card-packs). Admin API/UI: `GET /api/v1/admin/card-skins`, `skinId` в create/update card-pack, селект скина на `CardPacksPage`. TMA: `skinCode` в контрактах, helper `skinClass`, подключение skin-модификаторов во всех карточных местах (collection/team/modal/mini, marketplace list/feed/my-listings, leaderboard/history, pack opening), новый CSS-стиль `tournament_gold` (conic gold border, glow, shimmer, GOLD badge, tint perk chips). Добавлены интеграционные тесты в `UserApiIntegrationTest`: проверка выдачи скина из пака и сохранения скина через маркетплейс (listing/feed/transaction/buyer cards). Проверки: backend targeted tests + `npm run build` для `polemica-fantasy-admin` и `polemica-fantasy-webapp` — успешно.
- **2026-05-14 (TMA / complaint confirm modal):** На странице сделки `/marketplace/transactions/:listingId` (`TransactionDetailPage`) добавлено обязательное подтверждение перед отправкой жалобы: кнопка `Пожаловаться` открывает модалку с `Отмена/Отправить жалобу`. В модалке добавлены пояснения для пользователя: (1) администрация рассматривает жалобы только на транзакции маркетплейса, (2) действует дневной лимит жалоб, (3) при подтверждении нерыночной сделки жалобщикам может быть начислена награда. Состояние модалки закрывается после успешной отправки; стиль списка пояснений вынесен в `src/index.css` (`.pf-transaction-detail__complaint-notes`).
- **2026-05-14 (TMA / transaction detail compact card):** На `polemica-fantasy-webapp` странице сделки `/marketplace/transactions/:listingId` уменьшен визуальный вес карточки: ограничена максимальная ширина card-блока, уменьшены отступы/типографика подписи и чипов перков, слегка уплотнены отступы информационных секций. Изменения локализованы в `TransactionDetailPage.tsx` и CSS-блоке `.pf-transaction-detail*` в `src/index.css`, без влияния на карточки в других экранах. Проверка: `npm run build` (`polemica-fantasy-webapp`) — успешно.
- **2026-05-14 (admin / marketplace complaints UI):** Закрыт frontend-план `docs/plans/complaints/03-admin-frontend.md` в `polemica-fantasy-admin`: `MarketplaceModerationPage` расширена табами **«Жалобы»** и **«Игроки по жалобам»** (пагинация, фильтр `minComplaints`, статусы/действия), добавлены модалки санкции сделки и бана пользователя (пресеты 3/7/30, custom, перманент, разбан). В модалке санкции реализованы загрузка жалобщиков, рекомендуемые значения штрафов/наград по комиссии `marketplace.commission_percent`, предупреждение о переплате наград, подтверждение и вывод результата применения санкции. Обновлены админские TS-контракты (`api/types.ts`) и API-клиент (`api/marketplaceAdmin.ts`) для `complained-transactions`, `transactions/{id}/complaints`, `transactions/{id}/sanction`, `users-by-complaints`, `users/{telegramId}/ban`. Проверка: `npm run build` (`polemica-fantasy-admin`) — успешно.
- **2026-05-14 (TMA / marketplace complaints UI):** Закрыт frontend-план `docs/plans/complaints/02-tma-frontend.md` по `docs/features/DESIGN-MARKETPLACE-COMPLAINTS.md`: в `polemica-fantasy-webapp` добавлена страница сделки `/marketplace/transactions/:listingId` (`TransactionDetailPage`) с деталями продажи, участниками, блоком санкции и кнопкой жалобы (`GET/POST /api/v1/marketplace/transactions/{id}/*`). Обновлены контракты `api/types.ts` (`MarketplaceTransactionDetail`, `ComplainResult`, новые поля `listingId`/`sanctioned` в feed/profile-trades, `seller`/`card.value` nullable в каталоге), API-клиент `api/marketplace.ts` (fetch/complain transaction), роутинг `App.tsx`. На `MarketplacePage` лента сделок стала кликабельной (переход в деталку), санкционированные сделки помечаются бейджем и зачёркнутой ценой, в каталоге скрываются seller/value при `null`; на `PlayerProfilePage` «Последние сделки» стали кликабельными с меткой санкции. Добавлены CSS-стили для transaction detail и санкционных бейджей. Проверка: `npm run build` (`polemica-fantasy-webapp`) — успешно.
- **2026-05-14 (backend / marketplace complaints + sanctions):** Реализован backend-план `docs/plans/complaints/01-backend.md` по спецификации `docs/features/DESIGN-MARKETPLACE-COMPLAINTS.md`: Flyway **V39** (`marketplace_complaint`, `marketplace_listing_sanction`, `telegram_user.marketplace_banned_until`, `economy_config.marketplace.daily_complaint_limit`), сервисы `MarketplaceTransactionService` / `MarketplaceComplaintService` / `MarketplaceSanctionService`, событие `MarketplaceSanctionAppliedEvent` и listener уведомлений, расширение `MarketplaceAdminService` (жалобы/санкции/бан по сроку), новые user/admin DTO и endpoint'ы, а также anonymization публичных листингов (скрытие продавца и `card.value`). Добавлены тесты (`MarketplaceComplaintServiceTest`, сценарии в `UserApiIntegrationTest`, `AdminApiIntegrationTest`), целевой прогон по новому функционалу — зелёный.
- **2026-05-12 (TMA / TeamPage unlist UX correction):** По обратной связи убран верхний блок массового снятия листингов на `TeamPage`. Действие перенесено на саму карточку в сетке: для карт с `activeMarketplaceListing` под карточкой показывается кнопка **«Снять с продажи»** (точечный `DELETE /api/v1/marketplace/listings/{id}`), после успешного снятия инвалидаются `cards` / `my-marketplace-listings` / `marketplace-listings`.
- **2026-05-12 (TMA / TeamPage marketplace unlist UX fix):** Блок снятия листингов на экране сборки команды сделан постоянно видимым при наличии активных лотов (раньше появлялся только при выборе игрока в фильтре серии, что было неочевидно). Теперь над выбранными слотами показывается сводка «На продаже: N карт», отдельный селект `Игрок в листинге` (только игроки с активными лотами) и кнопка **«Снять игрока с листинга»** для массового снятия всех его лотов.
- **2026-05-12 (TMA / TeamPage marketplace unlist):** На экране сборки команды (`/series/:id/team`) добавлен блок управления листингами выбранного игрока серии. После выбора игрока в фильтре `Игрок серии` показывается число его активных листингов и кнопка **«Снять игрока с листинга»**, которая пакетно вызывает `DELETE /api/v1/marketplace/listings/{id}` для всех карточек этого игрока (`Promise.allSettled`). После выполнения инвалидаются кэши `cards` / `my-marketplace-listings` / `marketplace-listings`, UI сразу разблокирует карты для выбора в состав.
- **2026-05-12 (backend / recycle soft-delete):** Распыление карты (`POST /api/v1/me/cards/{id}/recycle`) переведено с hard-delete на soft-delete: добавлена колонка `user_card.deleted_at` (Flyway **V38**), в `CardLifecycleService.recycleCard` карта больше не удаляется физически, а помечается `deletedAt = now`; ACTIVE-листинги этой карты переводятся в `CANCELLED` через `MarketplaceListingRepository.cancelAllActiveByUserCardId`. История владения (`user_card_ownership_history`) и прошлые ссылки на карту в сериях остаются в БД. В `UserCardRepository` активные пользовательские выборки (`/me/cards`, attach team, legendary upgrade, marketplace create/update, глобальный рейтинг/подсчёты) фильтруют `deletedAt IS NULL`; в `TelegramUserRepository` админские счётчики `cardsInSeries` также исключают soft-deleted карты.
- **2026-05-12 (admin UX / series roster + marketplace):** На `SeriesDetailPage` (экран `Assign players`) добавлено действие **Remove player listings**: админ может выбрать конкретного `tournament_player` и снять с маркетплейса все его активные лоты. Backend: новый endpoint `POST /api/v1/admin/series/{seriesId}/players/{tournamentPlayerId}/unlist-marketplace`, сервис `SeriesService.unlistMarketplaceListingsForTournamentPlayer`, bulk-update в `MarketplaceListingRepository.cancelAllActiveByFantasyPlayerId` (статус `ACTIVE -> CANCELLED`). Возврат: `SeriesPlayerMarketplaceUnlistResultDto` (`cancelledListings`, ids, nickname). Покрытие: `AdminApiIntegrationTest#series admin can remove selected player listings from marketplace`.
- **2026-05-08 (UX / TMA TeamPage):** На `TeamPage` блок действий состава перемещён сразу под выбранные слоты: кнопка `Отправить/Обновить` и ссылка `Обзор серии` больше не рендерятся после всей сетки карт. Ссылка `Обзор серии` теперь ведёт на маршрут состава серии (`/series/:id/team?league=...`), а не на обзор/лидербордный экран.
- **2026-05-08 (UX / TMA leaderboard):** `TournamentLeaderboardPage` получил fallback-режим лиг: если `GET /api/v1/tournaments/{id}` возвращает пустые/неполные `series.leagues`, страница теперь всё равно показывает переключатель `MAIN/BUDGET` и запрашивает per-league таблицы через `/series/{id}/leagues/{code}/leaderboard`. Это гарантирует доступ к бюджетному лидерборду турнира даже при неполном payload по лигам.
- **2026-05-07 (bugfix / backend marketplace analytics):** Исправлено падение `GET /api/v1/marketplace/analytics/detail` (`ClassCastException: [Ljava.lang.Object; cannot be cast to Number` в `MarketplaceService.getAnalyticsDetail`). Причина: в одном из режимов JPA/Hibernate агрегат `COUNT/MIN/MAX` приходил как вложенный `Object[]` (`[[count,min,max]]`), а сервис ожидал плоский массив (`[count,min,max]`). В `MarketplaceService` добавлен устойчивый `unwrapAnalyticsStatsRow` + безопасные касты через `as? Number` с fallback для `activeCount`. Добавлен интеграционный тест `UserApiIntegrationTest` на сценарий с двумя активными листингами и вызовом `/marketplace/analytics/detail` (проверка `activeCount/min/max`, `recentSales=[]`, `avgSalePrice=null`).
- **2026-05-07 (память / UX TMA):** На `CardsPage` убрана оверлейная плашка сводки рынка (`N шт. от X₣`) с изображения карты в сетке коллекции — она больше не перекрывает чипы перков. Сводка перенесена внутрь модалки карточки рядом с действиями продажи (`Продать` / `Управлять листингом`). В модалке «Выставить на маркетплейс» добавлен явный вывод ошибки загрузки детальной аналитики (`/marketplace/analytics/detail`) вместо «тихого» отсутствия блока; включён `refetchOnMount: 'always'` для актуализации аналитики при открытии меню продажи.
- **2026-04-30 (память / UX):** Для companion-карты «Тюленчик» в `PackOpening` добавлен текстовый чип в месте перков: `+N фантики` (где `N = value` companion/developer-карты). Отрисовка работает и на шаге reveal, и в финальной summary-сетке, используя тот же слот, где у обычной карты показываются perk chips.
- **2026-04-30 (память / уточнение требований):** Поведение пасхалки в открытии пака изменено: визуальный companion «Тюленчик» теперь **начисляет фантики** при покупке пака — по `+value` соответствующей developer-карты (та же полная ценность, что в `openingCards` для companion). Начисление выполняется в `UserStoreService` через `UserService.addBalance(..., EASTER_EGG_BONUS)` и отражается в `BuyPackResponseDto.fantiki`. Логика без создания `user_card` сохранена (companion по-прежнему только display-элемент в `openingCards`). Интеграционные тесты `UserApiIntegrationTest` обновлены: проверяют итоговый баланс `1000 + Σ companion.value`.
- **2026-04-30 (память / реализация):** Открытие пака (`POST /api/v1/store/packs/{id}/buy`) получило отдельный display-контракт `openingCards`: backend теперь возвращает последовательность для анимации раскрытия, где после каждой выпавшей developer-карты (`easter_egg.developer_fantasy_player_id`) вставляется визуальный companion «Тюленчик». Companion **не** создаёт `user_card`, **не** начисляет фантики и не пишет `EASTER_EGG_BONUS`; используется только в UI открытия пака. Для companion берётся редкость и **полная ценность** соответствующей developer-карты (включая perk bonus), image URL — из `easter-egg.tyulenchik-image-url`. TMA `StorePage`/`PackOpening` переведены на `openingCards` (с fallback на legacy `cards`) и показывают companion и в reveal, и в summary. Добавлены интеграционные тесты `UserApiIntegrationTest` на single/multi developer-кейсы и проверку `value`/порядка `developer -> companion`; проверки: targeted `gradlew test` и `npm run build` — зелёные.
- **2026-04-29 (память / реализация):** Легендарный апгрейд получил пасхалку для конкретного `fantasy_player`: при совпадении с `economy_config.easter_egg.developer_fantasy_player_id` backend после обычного EPIC→LEGENDARY апгрейда начисляет бонус `easter_egg.developer_bonus_fantiki` (по умолчанию 500, транзакция `EASTER_EGG_BONUS`) и возвращает расширенный ответ `LegendaryUpgradeResponseDto` (`card` + `easterEgg`). В `application.yml` добавлен конфиг `easter-egg.tyulenchik-image-url` (`EASTER_EGG_TYULENCHIK_IMAGE_URL`) для картинки «Тюленьчик». TMA `LegendaryUpgradeWizard` обновлён: при наличии `easterEgg` вместо мгновенного закрытия показывает отдельный result-step с двумя легендарными карточками, текстом пасхалки и строкой `+N фантиков`. Flyway **V37**: новые ключи `easter_egg.developer_fantasy_player_id` и `easter_egg.developer_bonus_fantiki`.
- **2026-04-29 (память / bugfix финализации лиг):** Подтверждён и исправлен дефект `SeriesFinalizationService`: при финализации серии `cardsDecremented` считался корректно, но `uses_remaining` карточек не сохранялся в БД в сценарии «одна карта выставлена в `MAIN` и `BUDGET`». Причина — декремент выполнялся по объектам `UserCard`, пришедшим через граф `FantasyTeam`, без явного сохранения. Фикс: после декремента добавлен `userCardRepository.saveAll(cardById.values)`. Добавлен интеграционный тест `UserApiIntegrationTest#finalize series decrements uses by leagues count for same card` (MAIN+BUDGET+finalize), который падает на старом поведении (`expected 0, was 2`) и проходит после фикса.
- **2026-04-28 (память / UX TMA):** На `HomePage` блок «Состав на серию» больше не скрывает активные серии, если команды уже выставлены во всех лигах: показываются все серии с открытым дедлайном, чтобы пользователь видел активное событие и мог зайти на редактирование состава. Обновлены CTA по состоянию лиг (`Собрать` / `Изменить` / `Открыть`) и текст подсказки блока.
- **2026-04-28 (память / UX TMA):** В `CardsPage` для карт с `activeMarketplaceListing` добавлено единое действие **«Управлять листингом»**: в модалке доступны смена цены листинга и кнопка «Снять с продажи». Для модалки выставления карты исправлен источник нижней границы цены: используется `economy-info.marketplaceMinPrices`, а не `renewalCosts`.
- **2026-04-27 (память / bugfix TMA):** На `TeamPage` исправлен сценарий «кнопка отправки не активируется» из-за сброса локального выбора карт после позднего ответа `GET /me/fantasy-teams/{seriesId}?leagueCode=...` или повторного refetch. Добавлен флаг гидрации `teamSelectionHydrated`: инициализация `selected` из API теперь происходит один раз на связку `seriesId + league`, а при `null`-команде не затирает уже выбранные пользователем карты. Это устраняет ложный reset выбора в бюджетной лиге и сохраняет доступность кнопки отправки при валидном составе.
- **2026-04-27 (память / bugfix TMA):** Исправлены два UI-дефекта в `polemica-fantasy-webapp`: (1) `TournamentLeaderboardPage` переведён на per-league запросы (`/series/{id}/leagues/{code}/leaderboard`) и получил `LeagueTabs` + `?league=` в URL, поэтому при переходе из турнира теперь доступна и бюджетная лига в лидерборде по сериям; ссылки на команду игрока из этого экрана сохраняют `league` в query. (2) На `TeamPage` устранено наложение бейджей ценности и лиги карты: `pf-card-league-badge` сдвинут ниже value badge, добавлен модификатор `pf-card-league-badge--team-dead` для dead-карт.
- **2026-04-27 (память / docs+TMA):** Обновлён раздел «Лиги» в TMA-справке (`polemica-fantasy-webapp/src/pages/HelpPage.tsx`): добавлены полные правила бюджетной лиги для игроков (лимит по сумме `card_value`, расход uses по лигам, суммирование наград по лигам, связь с турнирным рейтингом). В тексте справки процент выплат бюджетной лиги теперь явно показывается из `economy-info` (`leagues.BUDGET.rewardScale`, источник — `economy_config`), без хардкода. Дизайн-документ `docs/features/DESIGN-CARD-VALUE-AND-LEAGUES.md` (раздел 8.4) синхронизирован с этим требованием.
- **2026-04-26 (память / реализация):** Для серий `POLEMICA_COMPETITION` добавлен фильтр по фазе игры (`series.game_phase`): значения `0|1|2`, `null` = учитывать все фазы. В create/update admin API добавлено поле `gamePhase` (create default = `0` при отсутствии в JSON; update поддерживает явное `null` через отслеживание присутствия поля), в `SeriesDto` и админских TS-контрактах поле проброшено в UI. `DefaultGameSyncService.fetchCompetitionPrepared` теперь учитывает phase-фильтр (по reference и по загруженной игре), для `STANDALONE` значение всегда принудительно `null`. Flyway **V36**: колонка `series.game_phase`, CHECK `0..2 | NULL`, бэкфилл `0` для существующих competition-серий. В админке (`SeriesFormModal`, `SeriesDetailPage`) добавлен селект phase (0/1/2/All phases).
- **2026-04-26 (память / реализация):** Закрыт frontend этап лиг (Plan `docs/plans/archive/06-leagues-frontend.md`) в `polemica-fantasy-webapp`: добавлены типы `SeriesLeagueInfo`/`SeriesLeagueBrief` и расширения `UserCardItem` (`leaguesInSeries`, `canJoinMoreLeagues`), `FantasyTeamDto`/`PublicFantasyTeam` (`leagueCode`), `EconomyInfo.leagues`; новый API-клиент `src/api/leagues.ts`; UI-компоненты `LeagueTabs` и `BudgetProgressBar`. Обновлены страницы `SeriesPage`, `LeaderboardPage`, `TeamPage` под `?league=` (default MAIN), per-league лидерборды и сборка команды через `/series/{id}/leagues/{code}/*` с бюджетным прогресс-баром и блокировкой карт по `valueCap`/uses. На `HomePage` и `TournamentPage` добавлены per-league статусы `✓/✗`; в `HelpPage` добавлен раздел «Лиги» (value cap и reward scale из `economy-info`). Для консистентности истории и публичной команды учтён `leagueCode` в `FantasyHistoryPage` и `LeaderboardPlayerTeamPage`. Сборка `npm run build` — зелёная.
- **2026-04-26 (память / реализация):** Реализован backend этап лиг (Plan `docs/plans/archive/05-leagues-backend.md`) с учётом текущей модели уведомлений: Flyway **V35** (`league`, `series_league`, миграция `fantasy_team.series_league_id`, seed MAIN/BUDGET и economy-ключи `league.*`, deprecated-маркер для `legendary.team.max_per_series`), новые сущности/репозитории `League`, `SeriesLeague`, `LeagueType`, `LeagueVisibility`, `LeagueRepository`, `SeriesLeagueRepository`; автосоздание SYSTEM-лиг для новых серий в `SeriesService.createSeries`; сервисы `LeagueService` и `LeagueAdminService`; user API лиг (`LeagueController`: `/api/v1/series/{id}/leagues`, per-league leaderboard/team/public-team endpoints), admin API лиг (`LeagueAdminController`: `GET/PUT /admin/leagues`, `POST/DELETE /admin/series/{id}/leagues`), legacy endpoints серии/`/me/fantasy-teams` расширены `leagueCode` (default MAIN). В `UserFantasyTeamService` добавлена валидация по правилам лиги (team size min/max, value cap, max legendary, uses across leagues), в `/me/cards?seriesId` добавлены аннотации `leaguesInSeries` и `canJoinMoreLeagues`. Финализация серии переведена на per-league расчёт в `SeriesFinalizationService` (награды с `reward_scale`, списание uses по `COUNT(DISTINCT series_league_id)`), событие `SeriesFinalizedNotificationEvent` и Telegram-текст обновлены на модель `LeagueResult[] + totalReward`. `EconomyInfoDto` дополнен `leagues`, `UserSeriesDetailDto`/`UserSeriesSummaryDto` — brief-данными лиг, `FantasyTeamDto`/`PublicFantasyTeamDto` — `leagueCode`. Обновлены unit-тесты `SeriesFinalizationServiceTest` и `SeriesFinalizationTelegramMessageTest`.
- **2026-04-26 (память / bugfix):** `CardLifecycleService.recycleCard` больше **не** возвращает 400 для карты в `ACTIVE`-листинге маркетплейса. Recycle теперь разрешён и удаляет листинги карты через `marketplaceListingRepository.deleteAllByUserCard_Id(...)` в той же транзакции. Добавлены/обновлены тесты: `CardLifecycleServiceTest` (unit) и интеграционный сценарий `POST /api/v1/me/cards/{id}/recycle` для карты в листинге.
- **2026-04-26 (память / реализация):** Кнопка batch-start перенесена с `TournamentDetailPage` на страницу списка турниров `TournamentsPage`: теперь действие **Start all UPCOMING** собирает `UPCOMING` серии из **всех** турниров и делает один `POST /api/v1/admin/series/batch-start` (единый батч уведомлений). На `TournamentDetailPage` кнопка удалена.
- **2026-04-25 (память / реализация):** В админке (`polemica-fantasy-admin`) на `TournamentDetailPage` добавлена кнопка **Start all UPCOMING**: одним вызовом `POST /api/v1/admin/series/batch-start` переводит все `UPCOMING` серии турнира в `ACTIVE` и триггерит агрегированное уведомление старта. Добавлены frontend-типы/контракты `BatchStartSeriesRequest` и `BatchStartSeriesResponseDto` (`src/api/seriesRequests.ts`, `src/api/types.ts`, `src/api/series.ts`), после запуска инвалидируются кэши списка серий и турниров.
- **2026-04-25 (память / реализация):** Plan notify/05 закрыт (TMA UI уведомлений): в `polemica-fantasy-webapp` добавлены типы `src/types/notifications.ts` и react-query хуки `src/api/notifications.ts` для `GET/PUT /settings/notifications`, `GET/PUT /settings/tournament-subscriptions`, `GET/POST/DELETE /settings/marketplace-watches` (в т.ч. optimistic update настроек/подписок). В `App.tsx` добавлены routes `/notifications`, `/notifications/tournaments`, `/notifications/marketplace-watches` и кнопка-колокольчик 🔔 в top bar. Реализованы страницы `NotificationSettingsPage`, `TournamentSubscriptionsPage`, `MarketplaceWatchesPage` (группировка 8 категорий, toggle/non-toggle, ссылки на подписки и фильтры, CRUD watch-фильтров с валидацией формы). На `MarketplacePage` добавлена контекстная кнопка «🔔 Отслеживать этот фильтр» с созданием watch из текущих фильтров и UX-обработкой `409` (дубликат) и `400` лимита. Для нового UI добавлены стили блоков уведомлений/переключателей/списков и CTA в `index.css`.
- **2026-04-25 (память / реализация):** Plan notify/04 закрыт: добавлены сущности и репозитории `MarketplaceWatchFilter` / `MarketplaceWatchPending`, публикация `MarketplaceListingCreatedEvent` в `MarketplaceService.createListing` (турниры игрока через `TournamentPlayerRepository.findDistinctTournamentIdsByFantasyPlayerId`). Реализован `MarketplaceWatchNotificationListener` (AFTER_COMMIT + `@Async`, matching по player/tournament/rarity/maxPrice, исключение продавца, учёт `bot_blocked` и preference `MARKETPLACE_WATCH`) с постановкой в pending-очередь. Добавлен `MarketplaceWatchScheduler` (fixedRate 5 минут, батч-агрегация pending в одно сообщение + кнопка открытия маркетплейса, очистка pending), а также user API CRUD фильтров `GET/POST/DELETE /api/v1/settings/marketplace-watches` через `MarketplaceWatchService` (+ DTO `CreateMarketplaceWatchRequest`, `MarketplaceWatchDtos`, лимит 10, валидация критериев и maxPrice, защита от дубликатов через 409). Покрытие: unit-тест `MarketplaceWatchServiceTest`.
- **2026-04-25 (память / реализация):** Plan notify/03 закрыт: добавлены `DeadlineReminder` + `DeadlineReminderRepository`, upsert `deadline_reminder` в `SeriesService` при create/update серии (пересчёт `remind_at = teamDeadline - 1h`, сброс `sent/sent_at/recipient_count` при переносе дедлайна в будущее). В `TelegramUserRepository` добавлен native-query `findDeadlineReminderRecipients` (фильтрация по `bot_blocked`, preference `TEAM_DEADLINE_REMINDER`, отсутствию `fantasy_team` в серии, подпискам на турнир с fallback «без подписок = все турниры»). Реализованы `DeadlineReminderService` (формирование текста, кнопка «Подать команду», mark sent с `recipientCount`) и `DeadlineReminderScheduler` (`@Scheduled` раз в 60с, обработка pending reminders с логированием ошибок по id). Тесты: `DeadlineReminderServiceTest` + интеграционный сценарий в `AdminApiIntegrationTest` (create/update upsert и reset sent-state).
- **2026-04-25 (память / реализация):** Plan notify/02 закрыт: user API настроек уведомлений и подписок (`GET/PUT /api/v1/settings/notifications`, `GET/PUT /api/v1/settings/tournament-subscriptions`), DTO и сервисы `NotificationSettingsService` / `TournamentSubscriptionService`; `NotificationCategory` расширен полем `description`. Добавлен `SeriesBatchStartedEvent`, listener `SeriesStartNotificationListener` (агрегированное сообщение о старте серии, кнопка «Подать команду» для одиночного старта, фильтрация получателей по `bot_blocked`, preference `SERIES_START`, подпискам на турниры с дефолтом «без подписок = все турниры»). В admin API добавлен `POST /api/v1/admin/series/batch-start` (`BatchStartSeriesRequest`/`BatchStartSeriesResponse`). В `SeriesService` реализован `batchStartSeries` и публикация того же event при одиночном `PUT /api/v1/admin/series/{id}` при переходе `UPCOMING -> ACTIVE`.
- **2026-04-25 (память / реализация):** Инфраструктура уведомлений (Plan notify/01): Flyway **V34** (`notification_preference`, `tournament_subscription`, `telegram_user.bot_blocked`, `deadline_reminder`, `marketplace_watch_filter`, `marketplace_watch_pending`), enum `NotificationCategory`, сущности/репозитории `NotificationPreference`/`TournamentSubscription`; Telegram inline keyboard (`InlineKeyboardMarkup`), `TelegramSendResult`, `TelegramBotApiClient.sendMessageSafe`; централизован `NotificationDeliveryService` (проверка enabled/token, `bot_blocked`, user preferences, retry на 429, mark blocked на 403, batch delay). Листенеры `SeriesFinalized`/`MarketplaceSale`/`PairBan`/`SeriesRosterReplacement` мигрированы на delivery service; добавлены кнопки через `NotificationButtonFactory` (`/series/{id}/leaderboard`, `/cards`). `SeriesFinalizedNotificationEvent` получил `seriesId`. Broadcast переведён на `NotificationDeliveryService.broadcast` через `TelegramBroadcastAsyncSender`; `AdminBroadcastNotificationService` больше не передаёт token/chatIds в sender. В `UserService.getOrCreateAndUpdateProfile` при входе в TMA сбрасывается `botBlocked = false`.
- **2026-04-22 (память / реализация):** **История парных санкций + превью изъятий (V29 + API + админка).** Flyway **V29** — `marketplace_pair_sanction_history` (пара `user_id_low` < `user_id_high`, `reason`, `fantiki_taken_low/high`, `cards_count_low/high`, `created_at`); JPA `MarketplacePairSanctionHistory`, `MarketplacePairSanctionHistoryRepository.findAllByOrderByCreatedAtDesc(Pageable)`. После успешного `ban-pair` в той же транзакции — одна строка в истории (канонический low/high: фантики и число карт с `partA`/`partB` по `idA` vs `idB`). **Read-only API:** `GET /api/v1/admin/marketplace/ban-pair/preview?userA&userB` — `BanPairPreviewDto` (порядок userA/B как в запросе: баланс, `fantikiToConfiscate`, `balanceAfter`, список карт к изъятию, та же логика что и `ban` без сайд-эффектов); `GET /api/v1/admin/marketplace/ban-pair/history` — `PagedPairSanctionHistoryDto` (по умолчанию `size=20`, новые сверху). `MarketplaceAdminService`: `loadPairUsers` (общая валидация пары), `getPairTrades` на нём. **Админка:** `getBanPairPreview` / `getBanPairHistory` в `api/marketplaceAdmin.ts`, типы в `types.ts`; `MarketplaceModerationPage` — таб **«История санкций»**, в модалке санкции — превью таблицей (кнопка «Подтвердить» неактивна, пока превью не загрузилось). События **до** релиза V29 в БД не попадают.
- **2026-04-22:** Санкция за перелив `POST /api/v1/admin/marketplace/ban-pair` **не** выставляет `telegram_user.marketplace_banned` и **не** отменяет ACTIVE-листинги. Обновлены `PairBanNotificationListener` (текст в Telegram: маркет не блокируется, лоты остаются) и `polemica-fantasy-admin` / `MarketplaceModerationPage` (pair sanctions, не «marketplace ban»). В `BanPairUserResultDto` поле `listingsCancelled` всегда **0**. `POST /unban/{telegramId}` снимает только `marketplace_banned` (насле­дие или сценарии, где флаг задан вне `ban-pair`). Реализация: `MarketplaceAdminService.sanctionsForOneUserInPair`.

## Текущий фокус
**Актуализация ban-pair (2026-05-19):** `ban-pair` больше не удаляет и не soft-delete'ит карты, не пишет `ADMIN_CARD_CONFISCATE`; карточный список в ответе/preview теперь информационный. Повторное списание предотвращается per-listing marker'ом `marketplace_listing_sanction`: `sumSellerReceivedForSalesTo`, `findSoldFromPartnerToBuyer` и preview-кандидаты исключают уже санкционированные SOLD-листинги. `marketplace_pair_sanction_history` остаётся аудитом запусков, но не используется как глобальный блок на всю пару, чтобы новые сделки после первого бана можно было санкционировать отдельно.

**Special card skins (post-implementation hardening):** базовая реализация `tournament_gold` завершена end-to-end (DB + API + admin + TMA + интеграционные тесты). Ближайший фокус — QA на стейдже/проде по пользовательским сценариям (пак -> коллекция -> листинг -> покупка -> лента/сделка), затем приоритизация следующего шага: либо CRUD скинов в админке, либо расширение библиотеки скинов.

**Marketplace complaints (rollout):** backend (`01-backend`), TMA frontend (`02-tma-frontend`) и admin frontend (`03-admin-frontend`) реализованы. Текущий этап — интеграционная проверка end-to-end сценария «жалоба → санкция → уведомления/баны» и подготовка к релизу.

**Антимодерация перелива (admin + V27 + V28/V29):** Flyway **V27** — `telegram_user.marketplace_banned` (запрет и выставлять, и покупать при `true` в `MarketplaceService.createListing` / `buyCard`). **V28** — `marketplace_pair_clearance` (модератор помечает пару «проверена»; не связано с санкциями). **V29** — `marketplace_pair_sanction_history` — аудит применённых `ban-pair` (см. Changelog 2026-04-22). `MarketplaceAdminService` + `MarketplaceAdminController` под `/api/v1/admin/marketplace`: `GET /pair-analysis` (агрегация SOLD по парам), `GET /pair-trades?userA&userB` (сделки между двумя по Telegram id; в `PairTradeDto` — `currentOwnerTelegramId`, `buyerStillOwnsCard`: совпадение владельца с покупателем по сделке), `GET /ban-pair/preview?userA&userB` (превью изъятий), `GET /ban-pair/history` (пагинированная история), `POST /ban-pair` (тело `BanPairRequest`: `telegramIdA`/`telegramIdB`/`reason` — у каждого в паре: списание **полного** нетто, полученного **от партнёра** по `MarketplaceListingRepository.sumSellerReceivedForSalesTo` (все SOLD-продажи в направлении «сам → партнёр»; **в т.ч.** если оригинальный покупатель карту уже перепродал — **деньги с продавца за эту сделку всё равно** взыскиваются) через `UserService.forceDeductBalance` / `forceDeductFantiki`, баланс может **уйти в минус**, `FantikiTransactionReason.ADMIN_PAIR_BAN`; **карты** — только если экземпляр сейчас на аккаунте **оригинального покупателя** у партнёра: `UserCardRepository.findUserCardsBoughtOnMarketplaceFromPartner` (в `EXISTS`: `ml.buyer.id = uc.telegramUser.id`; перепроданные третьим лицам **не** удаляются; в цикле изъятия — защитный `continue`, если `telegramUser` не self); удаление как в recycle-ветке по FK; `ADMIN_CARD_CONFISCATE` с `amount=0` на зафиксированной карте). **Санкция за перелив не ставит `marketplace_banned` и не отменяет активные листинги** — доступ к маркетплейсу и витрина пользователя не режутся. `POST /unban/{telegramId}` — снятие `marketplace_banned` (наследие/ручные случаи, когда флаг выставлен иначе). После `ban-pair` — `PairBanNotificationEvent` + `PairBanNotificationListener` (текст о санкции, что маркетплейс **не** блокируется и лоты **не** снимаются; причина, фантики, карты, баланс). В `BanPairUserResultDto` поле `listingsCancelled` остаётся **0** (совместимость API). Админка: `/marketplace-moderation` (`MarketplaceModerationPage`, `api/marketplaceAdmin.ts`, пункт меню **Marketplace**), колонка **Seize card at ban** (по смыслу — изъятие при санкции), таб **История санкций**, модалка санкции с превью и пояснениями; фантики и карты как выше, без бана и без mass-cancel листингов.

**Исключение из пула пака:** колонка `tournament_player.excluded_from_pack_pool` (Flyway **V20**), API `PATCH /api/v1/admin/tournaments/{id}/players/{playerId}` с телом `{"excludedFromPackPool": boolean}`; `CardPackService.buildFantasyPlayerPool` и валидация паков учитывают флаг. Админка: страница турнира, колонка «Пул пака» (Switch).

**Легендарные карты (backend L1–L3):** Flyway **V19** — `user_card.crafted_by_telegram_user_id` → `telegram_user`, ключи `economy_config` `legendary.upgrade.cost` (400), `legendary.team.max_per_series` (1). `UserCardItemDto.craftedByTelegramUserId` (Telegram platform id крафтера). `LegendaryUpgradeService`: EPIC → LEGENDARY in-place, +1 перк, списание `LEGENDARY_UPGRADE`, `findOrCreateCardTemplateForPerks` в `CardPackService`. API: `GET/POST /api/v1/legendary-upgrade` (+ `/info`). `UserFantasyTeamService.attachCards` — не больше `legendary.team.max_per_series` LEGENDARY в команде.

**Порядок серий в турнире (API + TMA):** `SeriesRepository.findAllByTournament_IdOrderByIdDesc` — в ответе `GET /api/v1/tournaments/{id}` и админском списке серий — **от новых к старым** (по `id` DESC). Лидерборд турнира (вкладки) и экран выбора серии (`/tournaments/:id/series`) берут порядок из API. Номер бейджа «Серия N» берётся из `series.public_number` / API `publicNumber`, чтобы публичный номер не зависел от порядка списка и технического `game_num_from`.

**Ростер серии и фэнтези:** при смене игроков серии до дедлайна `FantasyTeamRosterPruningService` убирает из `fantasy_team_card` карты игроков, которых больше нет в `series_player` (вызов из `assignPlayers` и при GET команды); Flyway **V17** — разовая чистка для `series_id = 5`. После удаления строк уплотнение слотов `1..n` делает **`saveAndFlush` по одной карточке** (а не `saveAll`), чтобы не ловить `23505` на `UNIQUE (fantasy_team_id, slot)` из‑за порядка SQL при одном flush. После **`assignPlayers`**, если карты срезаны — Telegram: **`SeriesRosterReplacementNotificationEvent`** (zip замен по id `tournament_player`, если |убрано| = |добавлено|).

**Бесплатные паки:** лимит задаётся на паке (`card_pack.free_opens_per_user`), учёт в `user_card_pack_free_usage`; админка — Card packs; TMA магазин показывает остаток и не требует баланса, пока есть квота.

**Планировщик серий:** `@EnableScheduling` на `FantasyApplication`; `schedule/ActiveSeriesSyncScheduler` — cron `0 0/10 * * * *` (каждые 10 мин), для серий `ACTIVE`/`SCORING` с `finalized = false` — `syncGames` + `calculateScores`; `SeriesRepository.findAllByStatusInAndFinalizedIsFalse`.

**Sync/scoring и Hikari:** HTTP к Polemica не внутри `@Transactional` — сначала загрузка/подготовка данных (`DefaultGameSyncService`: список `PreparedSeriesGame`; `DefaultScoringService`: `gamePointsService.fetchPlayerStats` по id игр), затем короткая транзакция записи через `TransactionTemplate` (`persistPreparedGames` / `applyScoresInTransaction`), чтобы не удерживать соединение пула на время сетевых вызовов.

**STANDALONE sync игр:** `fetchStandalonePrepared` — кандидаты по профилю, если match id есть у **≥ min(8, N)** игроков ростера (**N** = размер ростера), затем `name_prefix`, `loadMatch` и (если задано) совпадение календарного дня `game.started.toLocalDate()` с `series.game_started_on` (`gameStartedOn` в admin API). Константа overlap-порога: `STANDALONE_MIN_PLAYERS_IN_PROFILE_OVERLAP = 8`. Скрипт [`scripts/trace_series_game_sync.py`](../scripts/trace_series_game_sync.py) синхронизирован: поддерживает трассировку отсечения по started-дню и печатает отдельную статистику по этому фильтру (плюс `--min-overlap`, default 8).

**Отображаемое имя (TMA):** колонка `telegram_user.display_name`, `PATCH /api/v1/me` с телом `{"displayName":…}` (null/`""` — сброс); `first_name`/`username` по-прежнему синхронизируются из Telegram initData, кастомный ник не затирается. Лидерборд и публичная команда отдают `displayName` в `UserPublicDto`. Гонка при первом `INSERT` одного `telegram_id`: вставка + INITIAL фантиков в `TelegramUserBootstrapService.insertNewUserWithInitialFantiki` с `REQUIRES_NEW`, при `23505` — догрузка строки и обновление полей Telegram в основной транзакции. Flyway **V14**.

**Неполная команда на серию:** можно выставить 1–3 карты; награда за место при финализации масштабируется ⌈⅓⌉ / ⌈⅔⌉ / 100% от суммы из `economy_config` (см. `SeriesFinalizationService.scaleSeriesRewardByRosterSize`).

**Автофинализация при FINISHED:** при `PUT /admin/series/{id}` (и при создании серии со статусом FINISHED), если после сохранения `status == FINISHED` и `finalized == false`, вызывается `SeriesFinalizationService.finalizeSeries`. Кнопка `POST .../finalize` тоже ведёт в `finalizeSeries` и дополнительно выставляет `status = FINISHED` (награды + декремент uses + `finalized = true`).

**V3 (PLAN-V3) реализована по цепочке C1→C5:** контракты карт (`uses_remaining`, `times_renewed`), `series.finalized`, таблица `economy_config` + сиды; сервисы `EconomyConfigService`, `CardLifecycleService`, `SeriesFinalizationService`; user API recycle/renew/economy-info; admin finalize series + CRUD экономики; админка `/economy` и кнопка финализации серии; TMA — бейджи использований, коллекция (фильтры/сортировка, переработка/продление), **экран «Справка»** `/help` (механика очков, каталог перков `GET /api/v1/perks`, блок экономики из `economy-info`), редирект `/economy` → `/help`; сборка команды (истёкшие недоступны, предупреждение «последнее использование»). Архив плана: [`docs/plans/archive/PLAN-V3.md`](../docs/plans/archive/PLAN-V3.md). SDD: [`docs/architecture/DESIGN.md`](../docs/architecture/DESIGN.md).

**Коллекция — полная карточка:** на `/cards` по клику на область карточки открывается модалка как в лидерборде/истории фэнтези (полный список перков, «Очки в сериях» из `GET /me/fantasy-teams`, детализация по играм через `GET /me/fantasy-teams/{seriesId}/details`, переключатель серии при нескольких командах); переработка/продление/продажа — в модалке; общий компонент разбивки очков — `ScoreBreakdownBlock.tsx`.

**Деплой:** TMA `https://fantasy.maftourbot.ru`, админка `https://admin.fantasy.maftourbot.ru`, бэкенд в Docker (`docker-compose.prod.yml`). На сервере **`~/polemica-fantasy`** — **git clone** ветки **`master`** (`git@github.com:Vyasma-Mafia/polemica-fantasy.git`, SSH).

## Реализованные изменения V2
1. **Фантики** — внутриигровая валюта (стартовый баланс 1000, отображение в TMA, начисление через админку, `fantiki_transaction` для аудита)
2. **Перки** — справочник `perk` в БД (8 типов по эталону polemica-perk-service, V10); `voteForBlack` — `MULTIPLE_PER_GAME`; остальные — `ONCE_PER_GAME`; роли по смыслу перки; `can_appear_on_random_cards` — после V11 **все** записи каталога `TRUE` (random cards в паках); `PerkType` enum удалён; `CardTemplatePerk.bonus_points` nullable
3. **Автогенерация паков** — `card_pack.auto_generated`, пул игроков (`card_pack_player` / all tournament), генерация при открытии (Rare +1 перк, Epic +2, Legendary не участвует), переиспользование CardTemplate при совпадении
4. **Магазин паков** — `GET /store/packs`, `POST /store/packs/{id}/buy`, анимация открытия (PackOpening component)
5. **Модификатор редкости** — `Rarity.scoreModifier` (COMMON 1.0, RARE 1.1, EPIC 1.15, LEGENDARY 1.25)
6. **Per-game детализация** — `fantasy_team_card_game_score`, `fantasy_team_card_game_perk`, endpoint `GET /me/fantasy-teams/{seriesId}/details`

## Текущие решения
- Карточки привязаны к `fantasy_player` (глобальный), не к турниру
- Perk — справочник (не enum); бонус: `CardTemplatePerk.bonusPoints ?? Perk.bonusPoints`
- Паки: `probability` убрана, только `cards_count`; auto-gen: `applicableRoles` не фильтруется при генерации (только при скоринге)
- Язык бэкенда: Kotlin; Flyway V1–V14 (`V14__telegram_user_display_name`)
- Скоринг: `(base + Σperk) × rarity_modifier`, per-game breakdown хранится в БД; **базовые очки** — `GamePointsService.fetchPlayerStats(polemicaGameId)` (публичная страница `/match/{id}`, поле `points` по позиции за столом), не `PolemicaPlayer.award`; в расчёт попадают **только завершённые** игры (`PolemicaGame.result != null`); в `scored = true` помечаются только они
- Имена игр без заголовка из API: при sync — `Игра {num}` / `Игра #{polemicaGameId}`; для API — `formatSeriesGameDisplayName`
- S3: AWS SDK Java v2, MinIO в dev
- Образ backend: GHCR; на VPS — `docker compose -f docker-compose.prod.yml up -d --build`

## Следующие шаги
1. Опционально: серверный агрегат лидерборда по турниру (сейчас TMA суммирует на клиенте)
2. Опционально: доп. методы в polemica-library (оптимизация `getPlayerGames`)
3. Этап 2 баланса перков: по отчёту этапа 1 и prior ролей 6/2/1/1 задать формулу `bonus_points` (отдельная задача)

## Статистика перков (этап 1 баланса)
- **POST** `/api/v1/admin/perk-statistics/collect` (Basic Auth) — для каждого `fantasy_player` первая страница профиля Polemica (100 игр), дедуп `match_id`, `getMatch`, прогон тех же `PerkDetector` + `isRoleApplicable`, что в скоринге; ответ — агрегаты по каждому `perk.id` (`applicableSlots`, `sumRawMatchCount`, `slotsWithPositiveRaw`, `sumAppliedOccurrences`), метаданные и до 100 ошибок загрузки матча.
- `PolemicaIntegrationService.fetchProfileGamesFirstPageForStatistics` — page=1, limit=100.
- **2026-05-24:** отчёт дополнен поиском EV-аномалий `fantasy_player × perk`: считается baseline по всем загруженным завершённым player-slots, личная частота на игру игрока, условная частота по applicable roles, сглаженный `lift` и `excessBonusPerGame`; параметры `minPlayerGames`, `minApplicableSlots`, `priorGames`, `maxAnomalies` можно передавать в тело `POST /collect`.

## Открытые вопросы
- Финальная настройка бонусов перков (через админку)

## Блокеры
- Нет критичных блокеров

## 2026-08-11 — Telegram guarded create + result finalization flow

- Реализован default-off text-only импорт анонсов ЛП/ЗЛ: VPN-only Telethon
  SHADOW worker → HMAC evidence ingest → backend-owned preview/confirm buttons →
  атомарное создание `UPCOMING` shell-series без состава.
- Авторизация действий временно основана на exact operator chat; Telegram actor
  записывается в audit, а confirm привязан к тому же actor, но actor allowlist
  по продуктовому решению не добавлен.
- Добавлены Flyway V74 durable inbox/revisions/actions/callback inbox/source
  links/audit/outbox, защита от worker DB reset/edit/replay и общий tournament
  lock для create/update duplicate invariant.
- Добавлены per-policy `DISABLED|MANUAL|AUTOMATIC` для create/finalize,
  `expected_game_count`, durable reconcile/finalize jobs и exact RESULT linking
  только через ANNOUNCEMENT source link. Strict readiness проверяет ordered
  STANDALONE games, finished cache, полноту public points/scoring fingerprint и
  победителей; manual finalize использует same-actor confirmation, automatic —
  15m grace + 3 stable polls по 120s + generation/cutover recheck.
- OCR/albums, roster assignment, non-STANDALONE и correction/reopen отложены;
  production deployment не выполнялся, все write/result/mode flags default-off.

## 2026-08-14 — Telegram OCR roster vertical slice

- Добавлен default-off MANUAL-assisted путь для одного фото анонса: VPN-only
  worker скачивает JPEG/PNG, вызывает Yandex Vision `recognizeText` (`page`,
  `ru,en`) отдельным scoped key, сохраняет bounded OCR evidence в SQLite и
  удаляет raw bytes после durable transition. Albums/document/video и provider
  failures доставляются как `UNSUPPORTED`/`FAILED`, без silent text fallback.
- Flyway V76 разделяет text `content_hash` и immutable `evidence_hash`, хранит
  media/OCR evidence, roster draft/checksum и привязывает action/job к roster
  checksum. Поздний OCR старой revision проигрывает fencing/CAS; media-only edit
  инвалидирует старые кнопки.
- Backend сопоставляет OCR-строки только exact-unique с nickname существующего
  `tournament_player` настроенного турнира (NFKC/case/whitespace only). Нет
  fuzzy/transliteration/confusable substitution, создания игроков или reuse
  числовых Polemica aliases. Commentator/substitution markers, duplicate,
  ambiguity и неполный expected roster переводят item в `NEEDS_REVIEW` без
  production action.
- Успешный preview показывает каждое `OCR → production` соответствие; confirm
  привязан к exact chat/message/actor/source revision/evidence/ordered roster.
  Новая `UPCOMING` series, все `series_player`, source link, action/item, audit и
  success outbox коммитятся одной транзакцией. Existing-series roster rewrite
  отсутствует. Media/OCR create жёстко ограничен режимом `MANUAL`; `AUTOMATIC`
  для анонса с изображением создаёт только review без job.
- Worker и backend используют одинаковые fixed-vector canonical hashes для
  text-only, SUCCESS и terminal OCR evidence; source identity повторно
  проверяется после provider call. Explicit `reclassify-message` восстанавливает
  сохранённый SUCCESS после terminal backend 4xx без повторного OCR-вызова.
  Provider request преобразует evidence MIME `image/jpeg|image/png` в требуемые
  Vision enum `JPEG|PNG`; live VPN-container smoke с scoped key дал HTTP 200.
- Независимые backend flags `ocr-roster-enabled` и `roster-writes-enabled`, а
  также worker `TELEGRAM_IMPORT_OCR_ENABLED` default-off. Production rollout
  выполнен коммитом `cec1abe`: Flyway применил V76, backend и VPN-only worker
  healthy, OCR и ручная atomic roster write включены для ЛП/ЗЛ при сохранённом
  `create-mode=MANUAL`. Исторический backlog не переигрывался. Реальные
  изображения `2243`/`2247` прогнаны через production Yandex Vision и сохранены
  как text-only golden fixtures. Caption substitutions поддерживают bounded
  `Заменой X выступит Y` / `Вместо X выступит Y`: исходящий exact-unique среди
  OCR roster (с ограниченной русской родительной формой), входящий exact-unique
  в tournament roster, preview/checksum явно фиксируют `X → Y`. Для реального
  `2247` добавлены только explicit aliases `Градиент → Gradient` и
  `Cristo I → Cristo`; unsupported/ambiguous text остаётся `NEEDS_REVIEW`.

## 2026-05-25 — Achievements Stage 2 profile showcase
- Добавлена витрина достижений публичного профиля: summary claimed/visible, выбранная рамка профиля, featured badges, next achievement.
- Backend Stage 2: Flyway `V47__profile_showcase.sql`, сущности/репозитории `user_profile_customization` и `user_profile_featured_achievement`, `ProfileCustomizationService` + `/api/v1/me/profile-customization`.
- Правила Stage 2: featured badges только `claimed_at IS NOT NULL` и `visibility = 'PUBLIC'`; hidden/private не выбираются и не отдаются публично; disabled public claimed остаются видимыми только если уже featured; selected frame валидируется на read/write по `user_cosmetic_unlock.cosmetic_type = 'PROFILE_FRAME'`; reorder делает delete + flush + insert.
- TMA Stage 2: типы и hooks в achievements API, блок витрины в `PlayerProfilePage`, маршрут `/profile-customization` с выбором рамки и ordered featured badges.
- Verification: targeted `AchievementStage2ProfileShowcaseIntegrationTest` green; backend `compileKotlin compileTestKotlin --rerun-tasks` green; TMA `npm run build` green; `./scripts/codex-check.sh quick` green after Gradle cache escalation. Local Vite returned HTTP 200, but visual browser pass was not completed because fresh initData generation lacked `TELEGRAM_BOT_TOKEN` and Browser MCP was not exposed.

## Недавние правки UI (админка)
- **Series batch start (`/tournaments`):** в шапке списка турниров добавлена кнопка **Start all UPCOMING** с подтверждением; собирает `series.id` со статусом `UPCOMING` из **всех** турниров (`activeSeries`), вызывает `POST /api/v1/admin/series/batch-start`, показывает результат (started/skipped) и обновляет список турниров.
- **Маркетплейс — антимодерация:** страница `/marketplace-moderation`: таблица `pair-analysis`, детализация сделок пары (колонка **Seize card at ban** = `buyerStillOwnsCard` в `PairTradeDto`), модалка санкции пары (`ban-pair`) с причиной по умолчанию, **превью изъятий** (`GET ban-pair/preview`) до подтверждения, таб **История санкций** (`GET ban-pair/history`), модалка результата после применения, поле **Unban** по `telegramId` (POST unban, в т.ч. снятие унаследованного `marketplace_banned`). Тексты: санкции пары / перелив, без путаницы с глобальным `marketplace_banned`. API см. абзац «Антимодерация перелива» и Changelog выше.

- **User tools — списание фантиков:** `POST /api/v1/admin/users/{telegramUserId}/take-fantiki` с телом `{"amount": positive}`; причина транзакции `ADMIN_CONFISCATE`; пользователь должен существовать (иначе 404), при недостаточном балансе — 400. Кнопка «Take fantiki» на `/user-tools` (тот же amount, что и для grant).

- **Серии — sync/score счётчики:** `SeriesDto` содержит `syncedGamesCount` / `scoredGamesCount` (агрегаты по `series_game`); таблица серий на странице турнира и подзаголовок на странице серии. Список турниров — колонка **Active series** (все серии со статусом ≠ `FINISHED`, ссылки на `/series/:id`). **Finalize** (`SeriesFinalizationService`) выставляет `status = FINISHED`. На **Tournament detail** блок **Series** выше блока **Players**.
- **Tournament detail (`/tournaments/:id`):** над таблицей игроков — сетка карточек (`List` + `Card`) с аватаром (фото или первая буква ника), ником и внутренним id; в колонке Photo таблицы — миниатюра `Avatar` + ссылка «open» на полное изображение.
- **Users (`/users`):** список всех `telegram_user` с колонками username, Telegram ID, displayName; фильтры **Tournament** + **Series**; столбец **Cards (series)** — число экземпляров `user_card`, чья `card_template` относится к игроку из ростера серии (как `GET /me/cards?seriesId`). API: **GET** `/api/v1/admin/users` (без query — `cardsInSeries: null`) и с `tournamentId` + `seriesId` (оба обязательны вместе для счётчика). Реализация: `AdminUserListService`, нативный запрос в `TelegramUserRepository.findAllWithCardsInSeriesCount`.
- **Series → Assign players:** у `Select` включён поиск (`showSearch`) и фильтрация опций по подстроке (без учёта регистра), чтобы быстро находить игрока по нику в длинном списке.

## Недавние правки TMA (шапка приложения)
- **`App.tsx` / `index.css`:** шапка `header.top` — две логические строки: **`top__bar`** (бренд + `FantikiBalance` в одну линию), ниже **`nav`**; убран вложенный `flex-wrap`, из‑за которого на узкой колонке баланс уезжал на третью строку под «Магазин».
- **`TopBarDisplayName`:** между брендом и балансом — ник через `formatUserDisplayName` (`GET /me`, тот же `queryKey`, что у `FantikiBalance`); класс **`top__user`** — по центру гибкой колонки, обрезка длинного текста.

## Недавние правки TMA (главная)
- **Список серий с открытым составом:** `GET /api/v1/tournaments/series-open-for-team` — серии турниров со статусом `ACTIVE`, `team_deadline > now`, `series.status != FINISHED`, сортировка по дедлайну; DTO `SeriesOpenForTeamDto`. На `/` (`HomePage`) блок «Состав на серию», ссылка «Далее» → `/series/:id/team` с `state={{ fromHome: true }}`; кэш-ключ `['tournaments', 'series-open-for-team', initData]`. Первая загрузка: не рендерить контент, пока оба запроса не дали данные или ошибку (`isPending && data === undefined && !isError`) — иначе возможен пустой экран (TanStack Query v5). `TeamPage`: назад на `/` при `fromHome`, подпись «Турниры». `SeriesPickerPage`: валидный `tournamentId` и проверка `data` до использования.

## Недавние правки TMA (карты и коллекция)
- **Маркетплейс — фильтры листингов:** `GET /api/v1/marketplace/listings` — опционально `tournamentId` и/или `seriesId` (игрок карты должен быть в ростере турнира / серии); глобальный `fantasyPlayerId` — по шаблону карты. Справочник игроков для селекта: `GET /api/v1/fantasy-players` (`FantasyPlayerBriefDto`). TMA: отдельный селект «Игрок», турнир + серия («Весь турнир» без выбора серии).
- **Маркетплейс — лента сделок:** `GET /api/v1/marketplace/feed` в каждом элементе отдаёт `sellerDisplayName`, `buyerDisplayName`, `card` (`MarketplaceListingCardDto`: фото, редкость, перки как в листинге). На `/marketplace` в «Последние покупки» — «продавец → покупатель», справа в чипе превью карты (`pf-marketplace-feed__thumb` + `pf-mini-card--{rarity}`), портрет ~44×56px.
- **Маркетплейс / мои листинги:** блок `.pf-marketplace-card__meta` (цена, продавец, «Купить» / «Снять с продажи») получил `position: relative; z-index: 4`, фон и верхнюю границу — иначе декоративные `::before` / `::after` у `.pf-collection-card__frame` (до `z-index: 3` у legendary-crafted) перекрывали действия.
- **Модалки с фото карты:** у `.pf-modal__close` задан `z-index: 2`, чтобы крестик не уходил под блок `.pf-modal__img-frame` / картинку (у `img` был `z-index: 1`).
- **Коллекция (`CardsPage`):** кнопка «Переработать» в модалке карты доступна и при `usesRemaining === 0` (рядом с «Продлить контракт»); бэкенд это уже разрешал, ограничение было только в UI.
- **Коллекция (`CardsPage`):** в сетке карточек убран нижний тулбар «Продать / Переработать / Продлить» — те же действия остаются в модалке по клику на карту (`pf-modal__economy-actions`).
- **Коллекция (`CardsPage`):** фильтр по турниру — `<select>` с названиями из `GET /api/v1/tournaments` (кэш-ключ `['tournaments', initData]`, как на главной); опция «Все карты»; если в URL есть `tournamentId`, которого нет в списке активных турниров, показывается доп. опция «Турнир №{id}».
- **Карты:** общая палитра редкости — common матовый серый (`--pf-card-common`), rare ледяной cyan с лёгким свечением (`--pf-card-rare`); перки в виде чипов на оверлее фото (`CardPerkChips`) на коллекции, сборке команды, раскрытии пака и сводке пака; увеличены шрифты в подписи к карте.
- **Покупка пака:** ответ `POST …/store/packs/{id}/buy` мапится в `UserCardItemDto` через шаблоны из `CardTemplateRepository.findAllByIdWithPerksLoaded`, чтобы список перков не терялся при сборке DTO; на экране раскрытия частицы EPIC/LEGENDARY под текстом (`z-index` у `pf-pack-open__card-cap` выше, чем у `pf-pack-open__particles`). После сводки пака — кнопки «Купить ещё» (закрывает оверлей, открывает модалку подтверждения того же `packId`) и «В коллекцию» → `/cards`.

## Недавние правки (экономика TMA)
- **Справка `/help`:** в блоке «Экономика» подраздел «Маркетплейс» — комиссия %, минимум открытых паков до покупки (`minPackOpensBeforeMarketplacePurchase`), таблица мин/макс цены листинга по редкости из `economy-info` (`marketplaceMinPrices`, `marketplaceMaxPrices`).
- **`EconomyConfigService.buildEconomyInfo`:** строки наград за лидерборд серии формируются из БД (`economy_config`: `description` как подпись, `value` как сумма через тот же кэш, что и финализация); порядок ключей `series.reward.*` зафиксирован списком в сервисе (как и в `getSeriesReward`). Интеграционный тест: `UserApiIntegrationTest` — `GET economy-info returns…`.
- **Тиры топ-25 / топ-50:** Flyway **V21** — `series.reward.top25` (11–25 место), `series.reward.top50` (26–50); участие с **51+** места (`description` у `series.reward.participation` обновлена). `getSeriesReward` и список в `buildEconomyInfo` синхронизированы. Админка **Economy**: сортировка строк «Series rewards» в логическом порядке. Справка `/help`: пояснение диапазонов мест + динамический список из API.

## Недавние правки TMA (лидерборд и чужие команды)
- **Просмотр команды из лидерборда серии:** `GET /api/v1/series/{id}/users/{telegramId}/fantasy-team` и `.../fantasy-team/details`; в TMA маршрут `/series/:seriesId/leaderboard/player/:telegramId` (`LeaderboardPlayerTeamPage`), переход по строке лидерборда (серия и вкладка серии на лидерборде турнира; вкладка «Общий» без ссылки на команду).

## Недавние правки TMA (сборка команды на серию)
- **2026-07-03:** по жалобе `vadim_voland` подтверждён продовый race на вкладках лиг: в `ЧПШ. Серия 6` BUDGET был сохранён за ~1.2 секунды до MAIN теми же картами. `TeamPage` теперь хранит выбранные карты со scope `seriesId:leagueCode`, скрывает stale selection при смене лиги и блокирует submit/grid до гидрации состава текущей лиги.
- **Дедлайн состава — время:** `formatDateShortWithTime` в `tournamentDates.ts`; на главной («Состав на серию»), выборе серии турнира и карточке серии (`SeriesPage`) подписи «Доступно до» / «Дедлайн команды» включают часы и минуты (локаль браузера).
- **`GET /api/v1/me/cards`:** опциональный `seriesId` — только карты игроков из `series_player` (как при валидации fantasy-team); неизвестная серия → 404.
- **`SeriesPlayerEntryDto` / TMA types:** поле `fantasyPlayerId` для фильтра «игрок серии» на экране сборки.
- **`TeamPage`:** запрос карт с `tournamentId` + `seriesId`; вкладки редкости, селект игрока серии, сортировка сетки по убыванию редкости; синхронизация слотов с командой из API и `setQueryData` после сохранения.
- **Тест:** `UserApiIntegrationTest` — `GET me cards with seriesId returns only cards for players on series roster`.

## Недавние правки интеграции
- **polemica-library 1.8.2:** исправлен sync games — ответ `get-games` иногда отдаёт `mmr` объектом; в DTO добавлен `ProfileGameMmrDeserializer`. Сборка бэкенда: `../polemica-library` → `./gradlew publishToMavenLocal`, затем fantasy-backend подтянет 1.8.2 из `mavenLocal()` до публикации в Central.
- **polemica-library (локально):** `Invalid enum value: 0` при `GET /v1/matches/{id}` — в JSON голосов встречается `candidate: 0` (не место за столом 1–10). Исправление: `PolemicaVote.candidate` как `Position?`, десериализация `0` → `null`, доработка `GameUtils` (игнор таких строк в подсчётах). После правки — снова `publishToMavenLocal` / bump версии библиотеки.
- **polemica-library:** `MissingKotlinParameterException` на `referee` — в части ответов API поле отсутствует или `null`. Исправление: `PolemicaGame.referee` → `PolemicaUser?`.
