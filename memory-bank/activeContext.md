# Active Context

## Текущий фокус
**V2 — концептуальные изменения системы.** Подробная спецификация: [`CHANGES-V2.md`](../CHANGES-V2.md); план работ с разделением на агентов: [`PLAN-V2.md`](../PLAN-V2.md). База данных будет пересоздана (миграция данных не нужна, пользователей нет).

**Agent B1 выполнен:** Flyway `V5`–`V8` (фантики, справочник достижений + FK с карточек, автопаки/пул игроков, per-game score tables); JPA-сущности и репозитории; `AchievementDetector` по строковому `achievement.id`; DTO/API без enum достижений; паки без `probability`; фронты админки и TMA синхронизированы с новыми полями (`achievementId`, `achievementName`, конфиги паков только `cardsCount`).

**Agent B5 выполнен (admin frontend + минимальный backend для справочника):** `GET/PUT /api/v1/admin/achievements` (`AchievementAdminController`, `AchievementAdminService`, `AchievementApplicableRoleRepository`); в `CreateCardPackRequest`/`UpdateCardPackRequest` — поле `autoGenerated` (по умолчанию `false` при создании; `CardPackService` больше не форсит `true`). Админка: страница `/achievements`, расширенные формы паков (auto, цена, пул игроков, подсказка для auto), User Tools — начисление фантиков, шаблоны карт — выбор достижения из API без поля bonus; API-клиенты `achievements.ts`, `users.ts`, расширен `packs.ts`. Интеграционные тесты: в создание пака добавлен `autoGenerated: true` там, где нужен store/open-pack.

**Agent B3 выполнен (backend):** атомарные `UPDATE` баланса в `TelegramUserRepository` (+/- фантиков); `UserService`: `getBalance`, `addBalance`, `deductBalance` с записями в `fantiki_transaction` (знак суммы: + начисление, − списание); при первом создании пользователя — транзакция `INITIAL` +1000; `GET /api/v1/me` включает `fantiki`; админка `POST /api/v1/admin/users/{telegramUserId}/give-fantiki`; User Store `GET /api/v1/store/packs`, `POST /api/v1/store/packs/{id}/buy` (`UserStoreService` + `CardService.openPack`, ответ `BuyPackResponseDto` с `UserCardItemDto` для анимации); маппинг карт вынесен в `UserCardItemMapping.kt`.

**Agent B7 выполнен (TMA):** компонент `PackOpening` — полноэкранный overlay (`z-index: 200`), фазы «тряска/исчезновение пака» → поочерёдное раскрытие карт (stagger 700 ms) с эффектами по редкости (COMMON slide, RARE/EPIC/LEGENDARY flip + glow, EPIC/LEGENDARY particles) → сводка «Вы получили» и кнопка «В коллекцию». Стили в `polemica-fantasy-webapp/src/index.css` (префикс `pf-pack-open-`). `StorePage`: после успешной покупки имя пака из кэша списка паков; `PackOpening` с `key` по id карт; dismiss ведёт на `/cards`. Без новых npm-зависимостей (CSS animations).

Ключевые изменения V2:
1. **Фантики** — внутриигровая валюта (стартовый баланс 1000, отображение в TMA, начисление через админку)
2. **Достижения** — рефакторинг в справочник: бонус, occurrence_type, applicable_roles, can_appear_on_random_cards
3. **Автогенерация паков** — карточки генерируются при открытии из пула игроков; Rare +1 ачивка, Epic +2, Legendary не участвует
4. **Магазин паков** — покупка за фантики в TMA + анимация открытия
5. **Модификатор редкости** — COMMON ×1.0, RARE ×1.1, EPIC ×1.15, LEGENDARY ×1.25
6. **Per-game детализация** — подробная разбивка очков карточки по каждой игре серии

**Деплой (прежний):** TMA `https://fantasy.maftourbot.ru`, админка `https://admin.fantasy.maftourbot.ru`, бэкенд в Docker (`docker-compose.prod.yml`). На сервере **`~/polemica-fantasy`** — **git clone** ветки **`master`** (`git@github.com:Vyasma-Mafia/polemica-fantasy.git`, SSH).

**Agent A6 выполнен:** Admin SPA `polemica-fantasy-admin/` (Vite + React 19 + TS + Ant Design 6 + TanStack Query + React Router 7). Создание турнира с `POLEMICA_COMPETITION`: поле **Polemica competition ID** — `InputNumber` (прямой ввод id), без загрузки списка соревнований и без спиннера в селекте (`TournamentFormModal.tsx`). Basic Auth через форму логина → `sessionStorage` (`polemica_admin_basic_b64`), `api/client.ts` добавляет `Authorization` ко всем запросам. Страницы: турниры, деталь турнира (игроки, фото, серии), серия (редактирование, assign players, sync games, calculate scores), шаблоны карт, паки, user tools (give cards / open pack). Dev proxy `/api` → 8080. Дополнительно к A3: read API `GET /api/v1/admin/tournaments/{id}/series`, `GET /api/v1/admin/series/{id}`, `GET /api/v1/admin/card-packs` (опционально `tournamentId`) — для списков в UI.

**Agent A5 (предыдущий):** User API под `/api/v1` с `Authorization: tma <initData>`, TMA `polemica-fantasy-webapp/`.

## Текущие решения
- **Карточки не привязаны к турниру:** таблица `fantasy_player` (уникальный `polemica_user_id`), `card_template.fantasy_player_id`; `tournament_player` — только связь турнир ↔ игрок; состав серии и фэнтези-команда проверяются по участию `FantasyPlayer` в серии. Подробно: [`DESIGN.md`](../DESIGN.md) §4, §6; миграция `V3__fantasy_player_global_cards.sql`.
- Язык бэкенда: Kotlin (не Java)
- Админка: отдельное React веб-приложение (не TMA)
- Турнир: `TournamentKind` (`STANDALONE` | `POLEMICA_COMPETITION`); при втором — `polemica_competition_id` на турнире, серии задают диапазон `num`; см. [`DESIGN.md`](../DESIGN.md) §2, §7.
- Sync игр: ветвление в `DefaultGameSyncService` по `kind`; админка: выбор Competition и полей серии под тип турнира.
- Карточки выдаются только через админа (вручную или через паки)
- Скоринг запускается вручную через админку
- S3: AWS SDK Java v2, MinIO в dev с path-style и автосозданием bucket
- Образ backend: CI пушит в **GHCR** (`ghcr.io/<owner>/polemica-fantasy-backend`); на VPS prod-сборка чаще из **`docker-compose.prod.yml`** (`build:` локально на сервере после `git pull`). Секреты: только **`~/polemica-fantasy/.env`** (не в git)

## Следующие шаги
1. По желанию: **A2** доп. методы в polemica-library (оптимизация списка игр игрока) — не блокер
2. ~~Опционально: admin read API для состава серии~~ — сделано: `SeriesDto.tournamentPlayerIds`; в `assignPlayers` после `deleteAllBySeries_Id` вызывается `flush()`, чтобы не было конфликта UNIQUE при повторной вставке
3. Опционально: серверный агрегат лидерборда по турниру (сейчас TMA суммирует по сериям на клиенте)

## Открытые вопросы
- Точный список достижений (achievement types) — будет определён позже
- Формула базовых очков игрока (award vs GamePointsService) — нужно уточнить

## Недавно сделано (утилиты)
- **`scripts/import_closed_league_from_html.py`** — импорт HTML-вырезки таблицы закрытой лиги (как `zl.html`): парсинг `polemicaUserId` / ник / путь аватара → `POST /api/v1/admin/tournaments`, игроки, фото с `POLEMICA_SITE_BASE_URL` → `POST .../players/{id}/photo`. Зависимости: `scripts/requirements-import-closed-league.txt` (`requests`).

## Недавно сделано (TMA UX)
- **Карточки в TMA:** см. [`systemPatterns.md`](systemPatterns.md) (раздел Telegram Mini App) и [`DESIGN.md`](../DESIGN.md) §3.1.1 — фото игрока / арт шаблона, рамка по редкости; задокументировано в Memory Bank и DESIGN.
- **Webapp:** тёмная тема (DM Sans + Fraunces), хаб турнира, выбор серии («дни»), лидерборд турнира (вкладки «Общий» + серии, агрегация на клиенте), правила, история фэнтези (`GET /me/fantasy-teams`), коллекция с фильтрами и табами редкости, обновлённые Team / Series / Leaderboard страниц.
- **User API:** `GET /api/v1/tournaments/{id}/participants` — ростер турнира для страницы участников (`UserTournamentService` + `TournamentController`).

## Блокеры
- Нет критичных блокеров для A5/A6
