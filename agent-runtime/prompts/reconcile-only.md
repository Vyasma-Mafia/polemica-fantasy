This run is RECONCILE_ONLY because durable operation intents are unresolved.

Call fantasy_reconcile_operation once for each unresolved operation id; it performs typed read-back
and updates the durable journal only from definitive evidence without resending. Do not create a team, buy a pack, trade, renew,
recycle, merge, upgrade, claim, select, save, submit, or perform any other new write. If evidence is
ambiguous, leave the intent unresolved, record the ambiguity, and stop. Do not work around the block
with shell, HTTP, UI, direct database access, retries, or manual repair.
